schema_version = 1
registry_id = "markeitech-docstring-attributes"
registry_version = 1
section_name = "Markeitech Metadata"
maximum_section_bytes = 8192
maximum_occurrences_per_object = 32
maximum_key_bytes = 128
maximum_value_bytes = 2048

# No production attribute meaning is approved in version 1. The parser and typed projection are
# exercised with test-only registries. Future fields require Markeitect approval and one [[fields]]
# entry defining their exact type, cardinality, validation, and exposure policy.
