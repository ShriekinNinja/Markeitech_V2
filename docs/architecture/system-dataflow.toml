[manifest]
schema_version = 1
id = "markeitech-v3-system-dataflow"
title = "Markeitech V3 System and Data Flow"
description = "Offline reviewed representation of current, conditional, external, historical, and future architecture."
scope = "Repository architecture documentation only; never runtime configuration or live state."
authority = "architecture_representation_only"
not_runtime_configuration = true
checkout_commit = "b2c5bf41d00e43dea47369e569cd4f326ea758af"
owner = "Markeitect"
review_status = "proposed"
architecture_effective_at = "2026-08-31"
generator_contract_version = 1
default_profile = "profile.v3-es-minimal"
limitations = ["Static source and configuration checks cannot prove connected runtime behavior.", "Provider account, entitlement, adapter request mapping, and live delivery remain unknown unless separately measured.", "Generated artifacts are documentation projections and must never be edited or treated as authority.", "Markeitech is read-only and advisory; no current order submission or execution exists."]

[[evidence]]
id = "evidence.current-status"
evidence_class = "tracked_authority"
source_path = "docs/current-status.md"
observed_at = "2026-08-31"
proves = ["implemented/current boundaries", "canonical calendar cutover", "bounded canonical-calendar connected acceptance", "removed visual-review actors"]
limitations = ["Status prose may lag code; conflicts are recorded explicitly."]

[[evidence]]
id = "evidence.development-guidelines"
evidence_class = "tracked_authority"
source_path = "docs/development-guidelines.md"
observed_at = "2026-08-30"
proves = ["offline and review procedure"]
limitations = []

[[evidence]]
id = "evidence.composition"
evidence_class = "source"
source_path = "src/markeitech/system/composition.py"
source_symbol = "ActorRegistration"
observed_at = "2026-08-30"
proves = ["actor roster", "composition policies", "composition ordering"]
limitations = ["Registration order is not an event-delivery or readiness guarantee."]

[[evidence]]
id = "evidence.node"
evidence_class = "source"
source_path = "src/markeitech/system/node.py"
source_symbol = "build_live_node"
observed_at = "2026-08-30"
proves = ["LiveNode and native engine boundary", "IB data-client registration"]
limitations = ["Static construction does not prove a successful provider connection."]

[[evidence]]
id = "evidence.profile"
evidence_class = "configuration"
source_path = "config/system.v3-es-minimal.toml"
observed_at = "2026-08-30"
proves = ["named active-profile enablement", "configuration conditions"]
limitations = ["Requested realtime mode does not prove received realtime data."]

[[evidence]]
id = "evidence.system-contracts"
evidence_class = "source"
source_path = "src/markeitech/system/messages.py"
observed_at = "2026-08-30"
proves = ["system signal identities"]
limitations = []

[[evidence]]
id = "evidence.historical-contracts"
evidence_class = "source"
source_path = "src/markeitech/acquisition/historical_messages.py"
observed_at = "2026-08-30"
proves = ["historical request and response identities"]
limitations = ["Installed adapter request-ID and cancellation mapping remain unknown."]

[[evidence]]
id = "evidence.calendar-contracts"
evidence_class = "source"
source_path = "src/markeitech/intelligence/calendar_messages.py"
observed_at = "2026-08-30"
proves = ["calendar transition identity", "calendar projection request and response identities", "typed per-calendar projection failure"]
limitations = ["Static contracts do not prove runtime delivery or connected-provider behavior."]

[[evidence]]
id = "evidence.canonical-calendar"
evidence_class = "source"
source_path = "src/markeitech/intelligence/session.py"
source_symbol = "CanonicalCalendar"
observed_at = "2026-08-30"
proves = ["pure mcal-backed calendar evaluator", "calendar definition identity", "source-recorded terminal-break normalization"]
limitations = ["Calendar projections are transient and bounded; they are not persisted market observations."]

[[evidence]]
id = "evidence.historical-planner"
evidence_class = "source"
source_path = "src/markeitech/system/historical_planner.py"
source_symbol = "HistoricalEvidencePlannerActor"
observed_at = "2026-08-30"
proves = ["semantic historical demand planning", "calendar projection consumption", "exact UTC historical request-plan publication"]
limitations = ["The planner does not call providers or own provider pacing."]

[[evidence]]
id = "evidence.intelligence-contracts"
evidence_class = "source"
source_path = "src/markeitech/intelligence/messages.py"
observed_at = "2026-08-30"
proves = ["intelligence signal identities"]
limitations = []

[[evidence]]
id = "evidence.metric-contracts"
evidence_class = "source"
source_path = "src/markeitech/intelligence/metrics.py"
observed_at = "2026-08-30"
proves = ["metric custom-data identity"]
limitations = []

[[evidence]]
id = "evidence.completed-bar-contract"
evidence_class = "source"
source_path = "src/markeitech/intelligence/completed_bars.py"
observed_at = "2026-08-30"
proves = ["completed-bar custom-data identity"]
limitations = []

[[evidence]]
id = "evidence.entity-contracts"
evidence_class = "source"
source_path = "src/markeitech/intelligence/entities.py"
observed_at = "2026-08-30"
proves = ["entity custom-data identities"]
limitations = []

[[evidence]]
id = "evidence.resource-contracts"
evidence_class = "source"
source_path = "src/markeitech/system/resource_contracts.py"
observed_at = "2026-08-30"
proves = ["resource signal identities"]
limitations = []

[[evidence]]
id = "evidence.persistence"
evidence_class = "source"
source_path = "src/markeitech/system/persistence.py"
source_symbol = "OperationalPersistenceActor"
observed_at = "2026-08-30"
proves = ["bounded asynchronous operational persistence boundary", "persistence readiness response"]
limitations = ["PostgreSQL internals are intentionally unmodeled; the diagram stops at the application write boundary."]

[[evidence]]
id = "evidence.discord"
evidence_class = "source"
source_path = "src/markeitech/system/discord.py"
source_symbol = "DiscordHealthActor"
observed_at = "2026-08-30"
proves = ["optional queued Discord projection"]
limitations = ["Disabled in the active profile."]

[[evidence]]
id = "evidence.visual-debug"
evidence_class = "source"
source_path = "src/markeitech/intelligence/visual_debug_capture_actor.py"
source_symbol = "VisualDebugCaptureActor"
observed_at = "2026-08-30"
proves = ["passive filesystem projection"]
limitations = ["Enabled but not yet accepted as architectural or analytical evidence."]

[[evidence]]
id = "evidence.roadmap"
evidence_class = "tracked_authority"
source_path = "docs/roadmap/v2-stage-9d-entities-rolling-state-plan.md"
observed_at = "2026-08-30"
proves = ["future governed advisory boundaries"]
limitations = ["Roadmap content is not current implementation."]

[[profiles]]
id = "profile.v3-es-minimal"
config_path = "config/system.v3-es-minimal.toml"
config_schema_version = 23
content_sha256 = "5165c6b2ed3a2827d0f762dc510edcc6df8b5820cb2af1067aa4d31e992f63ff"
status = "tracked_active_review_profile"
limitations = ["Represents the named tracked profile, not all available capabilities or a connected runtime observation."]
evidence = ["evidence.profile"]

[[boundaries]]
id = "boundary.process"
label = "Markeitech V2 process"
kind = "process"
temporal_status = "current"
evidence = ["evidence.node"]
limitations = []

[[boundaries]]
id = "boundary.nautilus"
label = "Nautilus LiveNode and native engines"
kind = "nautilus_engine"
parent = "boundary.process"
temporal_status = "current"
evidence = ["evidence.node"]
limitations = []

[[boundaries]]
id = "boundary.system"
label = "System and infrastructure actors"
kind = "actor_runtime"
parent = "boundary.process"
temporal_status = "current"
evidence = ["evidence.composition"]
limitations = []

[[boundaries]]
id = "boundary.acquisition"
label = "Data acquisition actors"
kind = "actor_runtime"
parent = "boundary.process"
temporal_status = "current"
evidence = ["evidence.composition"]
limitations = []

[[boundaries]]
id = "boundary.intelligence"
label = "Processing, metrics, intelligence, and state"
kind = "actor_runtime"
parent = "boundary.process"
temporal_status = "current"
evidence = ["evidence.composition"]
limitations = []

[[boundaries]]
id = "boundary.workers"
label = "In-process workers and queues"
kind = "worker"
parent = "boundary.process"
temporal_status = "current"
evidence = ["evidence.persistence"]
limitations = []

[[boundaries]]
id = "boundary.providers"
label = "External providers"
kind = "provider"
temporal_status = "current"
evidence = ["evidence.node"]
limitations = []

[[boundaries]]
id = "boundary.persistence"
label = "Operational persistence"
kind = "persistence"
temporal_status = "current"
evidence = ["evidence.persistence"]
limitations = []

[[boundaries]]
id = "boundary.projections"
label = "External projections and operator"
kind = "projection"
temporal_status = "current"
evidence = ["evidence.current-status"]
limitations = []

[[boundaries]]
id = "boundary.future"
label = "Future governed advisory and controlled boundaries"
kind = "future_governed"
temporal_status = "future"
evidence = ["evidence.roadmap"]
limitations = []

[[boundaries]]
id = "boundary.historical"
label = "Historical removed and rejected runtime identities"
kind = "actor_runtime"
temporal_status = "historical"
evidence = ["evidence.current-status"]
limitations = ["Historical inventory only; never current runtime behavior."]

[[components]]
id = "actor.system-control"
label = "System Control"
kind = "markeitech_actor"
logical_area = "system and infrastructure"
boundary = "boundary.system"
implementation_ref = "src/markeitech/system/actor.py:SystemControlActor"
composition_key = "system_control"
composition_order = 1
actor_id = "SYSTEM-CONTROL"
responsibilities = ["System Control responsibilities and lifecycle"]
semantic_owner = "actor.system-control"
mutation_owner = "none_current"
transport_owner = "none_current"
persistence_owner = "none_current"
recovery_owner = "none_current"
projection_owner = "none_current"
policy_owner = "operator.markeitect"
implementation_state = "implemented"
composition_policy = "always"
temporal_status = "current"
acceptance_state = "accepted"
evidence_certainty = "verified_source"
failure_isolation = "Nautilus actor callback boundary; failure is surfaced according to component policy."
cardinality = "one when composed"
style = "style.component.current"
profile_states = [{ profile = "profile.v3-es-minimal", enablement = "enabled" }]
evidence = ["evidence.composition"]
limitations = []

[[components]]
id = "actor.session-state"
label = "Session State"
kind = "markeitech_actor"
logical_area = "data processing and state"
boundary = "boundary.intelligence"
implementation_ref = "src/markeitech/intelligence/actors.py:SessionStateActor"
composition_key = "session_state"
composition_order = 2
actor_id = "SESSION-STATE"
responsibilities = ["Sole runtime owner of active CanonicalCalendar instances.", "Publish definition-identified calendar transitions and bounded immutable projections.", "Contain ordinary per-calendar projection construction failures without redefining provider data."]
semantic_owner = "actor.session-state"
mutation_owner = "none_current"
transport_owner = "none_current"
persistence_owner = "none_current"
recovery_owner = "none_current"
projection_owner = "none_current"
policy_owner = "operator.markeitect"
implementation_state = "implemented"
composition_policy = "always"
temporal_status = "current"
acceptance_state = "accepted"
evidence_certainty = "verified_source"
failure_isolation = "Nautilus actor callback boundary; failure is surfaced according to component policy."
cardinality = "one when composed"
style = "style.component.current"
profile_states = [{ profile = "profile.v3-es-minimal", enablement = "enabled" }]
evidence = ["evidence.composition", "evidence.calendar-contracts", "evidence.canonical-calendar"]
limitations = ["Global system READY does not currently assert calendar-projection readiness."]

[[components]]
id = "component.canonical-calendar"
label = "Canonical Calendar"
kind = "engine"
logical_area = "data processing and state"
boundary = "boundary.intelligence"
implementation_ref = "src/markeitech/intelligence/session.py:CanonicalCalendar"
responsibilities = ["Evaluate one immutable versioned calendar definition from admitted mcal schedule facts and configured product phases.", "Produce UTC exchange segments, phase windows, trade-date assignments, and bounded projections.", "Record source-cited corrections and the exact admitted terminal-break normalization outcome."]
semantic_owner = "actor.session-state"
mutation_owner = "none_current"
transport_owner = "none_current"
persistence_owner = "none_current"
recovery_owner = "none_current"
projection_owner = "actor.session-state"
policy_owner = "operator.markeitect"
implementation_state = "implemented"
composition_policy = "not_composed"
temporal_status = "current"
acceptance_state = "bounded_evidence"
evidence_certainty = "verified_source"
failure_isolation = "Pure evaluator owned inside SessionStateActor; ordinary per-calendar projection failures are converted to typed response failures."
cardinality = "one per active configured calendar ID, owned by SessionStateActor"
style = "style.component.current"
profile_states = [{ profile = "profile.v3-es-minimal", enablement = "enabled" }]
evidence = ["evidence.canonical-calendar"]
limitations = ["mcal authors admitted exchange-schedule facts; Markeitech-defined product phases remain separate configured semantics."]

[[components]]
id = "actor.evidence-health"
label = "Evidence Health"
kind = "markeitech_actor"
logical_area = "data processing and state"
boundary = "boundary.intelligence"
implementation_ref = "src/markeitech/intelligence/actors.py:EvidenceHealthActor"
composition_key = "evidence_health"
composition_order = 3
actor_id = "EVIDENCE-HEALTH"
responsibilities = ["Evaluate observation freshness and availability.", "Consume definition-identified calendar context without owning mcal.", "Use a bounded correlated one-shot projection request lifecycle for startup context."]
semantic_owner = "actor.evidence-health"
mutation_owner = "none_current"
transport_owner = "none_current"
persistence_owner = "none_current"
recovery_owner = "none_current"
projection_owner = "none_current"
policy_owner = "operator.markeitect"
implementation_state = "implemented"
composition_policy = "always"
temporal_status = "current"
acceptance_state = "accepted"
evidence_certainty = "verified_source"
failure_isolation = "Nautilus actor callback boundary; failure is surfaced according to component policy."
cardinality = "one when composed"
style = "style.component.current"
profile_states = [{ profile = "profile.v3-es-minimal", enablement = "enabled" }]
evidence = ["evidence.composition", "evidence.calendar-contracts"]
limitations = ["Calendar projection delivery is local and transient; no replay guarantee is claimed."]

[[components]]
id = "actor.discord-health"
label = "Discord Health Projection"
kind = "markeitech_actor"
logical_area = "actions and projections"
boundary = "boundary.system"
implementation_ref = "src/markeitech/system/discord.py:DiscordHealthActor"
composition_key = "discord_health"
composition_order = 4
actor_id = "DISCORD-HEALTH"
responsibilities = ["Discord Health Projection responsibilities and lifecycle"]
semantic_owner = "actor.discord-health"
mutation_owner = "none_current"
transport_owner = "none_current"
persistence_owner = "none_current"
recovery_owner = "none_current"
projection_owner = "none_current"
policy_owner = "operator.markeitect"
implementation_state = "implemented"
composition_policy = "conditional"
temporal_status = "current"
acceptance_state = "accepted"
evidence_certainty = "verified_source"
configuration_path = "discord.enabled"
failure_isolation = "Nautilus actor callback boundary; failure is surfaced according to component policy."
cardinality = "one when composed"
style = "style.component.disabled"
profile_states = [{ profile = "profile.v3-es-minimal", enablement = "disabled" }]
evidence = ["evidence.discord"]
limitations = []

[[components]]
id = "actor.visual-debug"
label = "Visual Debug Capture"
kind = "markeitech_actor"
logical_area = "actions and projections"
boundary = "boundary.intelligence"
implementation_ref = "src/markeitech/intelligence/visual_debug_capture_actor.py:VisualDebugCaptureActor"
composition_key = "visual_debug_capture"
composition_order = 5
actor_id = "VISUAL-DEBUG-CAPTURE"
responsibilities = ["Visual Debug Capture responsibilities and lifecycle"]
semantic_owner = "actor.visual-debug"
mutation_owner = "none_current"
transport_owner = "none_current"
persistence_owner = "none_current"
recovery_owner = "none_current"
projection_owner = "none_current"
policy_owner = "operator.markeitect"
implementation_state = "implemented"
composition_policy = "conditional"
temporal_status = "current"
acceptance_state = "unaccepted"
evidence_certainty = "verified_source"
configuration_path = "visual_debug_capture.enabled"
failure_isolation = "Nautilus actor callback boundary; failure is surfaced according to component policy."
cardinality = "one when composed"
style = "style.component.disabled"
profile_states = [{ profile = "profile.v3-es-minimal", enablement = "disabled" }]
evidence = ["evidence.visual-debug"]
limitations = ["Passive diagnostic projection; not architecture or analytical authority."]

[[components]]
id = "actor.quote-quality"
label = "Quote Quality Metrics"
kind = "markeitech_actor"
logical_area = "metrics and intelligence"
boundary = "boundary.intelligence"
implementation_ref = "src/markeitech/intelligence/quote_metric_actor.py:QuoteQualityMetricsActor"
composition_key = "quote_quality_metrics"
composition_order = 6
actor_id = "QUOTE-QUALITY-METRICS"
responsibilities = ["Quote Quality Metrics responsibilities and lifecycle"]
semantic_owner = "actor.quote-quality"
mutation_owner = "none_current"
transport_owner = "none_current"
persistence_owner = "none_current"
recovery_owner = "none_current"
projection_owner = "none_current"
policy_owner = "operator.markeitect"
implementation_state = "implemented"
composition_policy = "conditional"
temporal_status = "current"
acceptance_state = "accepted"
evidence_certainty = "verified_source"
configuration_path = "metrics.quote_quality.enabled"
failure_isolation = "Nautilus actor callback boundary; failure is surfaced according to component policy."
cardinality = "one when composed"
style = "style.component.disabled"
profile_states = [{ profile = "profile.v3-es-minimal", enablement = "disabled" }]
evidence = ["evidence.composition"]
limitations = []

[[components]]
id = "actor.session-metrics"
label = "Session Metrics"
kind = "markeitech_actor"
logical_area = "metrics and intelligence"
boundary = "boundary.intelligence"
implementation_ref = "src/markeitech/intelligence/session_metric_actor.py:SessionMetricsActor"
composition_key = "session_metrics"
composition_order = 7
actor_id = "SESSION-METRICS"
responsibilities = ["Converge bounded historical and live bars into completed-bar, session, window, and rolling measurements.", "Consume immutable calendar projections and transitions without instantiating mcal.", "Publish live and historical evidence demands for configured metric capabilities."]
semantic_owner = "actor.session-metrics"
mutation_owner = "none_current"
transport_owner = "none_current"
persistence_owner = "none_current"
recovery_owner = "none_current"
projection_owner = "none_current"
policy_owner = "operator.markeitect"
implementation_state = "implemented"
composition_policy = "conditional"
temporal_status = "current"
acceptance_state = "unaccepted"
evidence_certainty = "verified_source"
configuration_path = "metrics.session_measurements.enabled"
failure_isolation = "Nautilus actor callback boundary; failure is surfaced according to component policy."
cardinality = "one when composed"
style = "style.component.disabled"
profile_states = [{ profile = "profile.v3-es-minimal", enablement = "disabled" }]
evidence = ["evidence.composition", "evidence.calendar-contracts"]
limitations = ["The actor retains faulty combined responsibilities, is disabled in tracked profiles, and awaits the separately reviewed V3-03 replacement."]

[[components]]
id = "actor.session-reference-entities"
label = "Session Reference Entities"
kind = "markeitech_actor"
logical_area = "metrics and intelligence"
boundary = "boundary.intelligence"
implementation_ref = "src/markeitech/intelligence/session_entity_actor.py:SessionReferenceEntityActor"
composition_key = "session_reference_entities"
composition_order = 8
actor_id = "SESSION-REFERENCE-ENTITIES"
responsibilities = ["Session Reference Entities responsibilities and lifecycle"]
semantic_owner = "actor.session-reference-entities"
mutation_owner = "none_current"
transport_owner = "none_current"
persistence_owner = "none_current"
recovery_owner = "none_current"
projection_owner = "none_current"
policy_owner = "operator.markeitect"
implementation_state = "implemented"
composition_policy = "conditional"
temporal_status = "current"
acceptance_state = "accepted"
evidence_certainty = "verified_source"
configuration_path = "metrics.entity_analysis.enabled"
failure_isolation = "Nautilus actor callback boundary; failure is surfaced according to component policy."
cardinality = "one when composed"
style = "style.component.disabled"
profile_states = [{ profile = "profile.v3-es-minimal", enablement = "disabled" }]
evidence = ["evidence.composition"]
limitations = []

[[components]]
id = "actor.market-state-entities"
label = "Market State Entities"
kind = "markeitech_actor"
logical_area = "metrics and intelligence"
boundary = "boundary.intelligence"
implementation_ref = "src/markeitech/intelligence/market_state_actor.py:MarketStateEntityActor"
composition_key = "market_state_entities"
composition_order = 9
actor_id = "MARKET-STATE-ENTITIES"
responsibilities = ["Market State Entities responsibilities and lifecycle"]
semantic_owner = "actor.market-state-entities"
mutation_owner = "none_current"
transport_owner = "none_current"
persistence_owner = "none_current"
recovery_owner = "none_current"
projection_owner = "none_current"
policy_owner = "operator.markeitect"
implementation_state = "implemented"
composition_policy = "conditional"
temporal_status = "current"
acceptance_state = "accepted"
evidence_certainty = "verified_source"
configuration_path = "metrics.entity_analysis.enabled"
failure_isolation = "Nautilus actor callback boundary; failure is surfaced according to component policy."
cardinality = "one when composed"
style = "style.component.disabled"
profile_states = [{ profile = "profile.v3-es-minimal", enablement = "disabled" }]
evidence = ["evidence.composition"]
limitations = []

[[components]]
id = "actor.market-structure-entities"
label = "Market Structure Entities"
kind = "markeitech_actor"
logical_area = "metrics and intelligence"
boundary = "boundary.intelligence"
implementation_ref = "src/markeitech/intelligence/market_structure_actor.py:MarketStructureEntityActor"
composition_key = "market_structure_entities"
composition_order = 10
actor_id = "MARKET-STRUCTURE-ENTITIES"
responsibilities = ["Market Structure Entities responsibilities and lifecycle"]
semantic_owner = "actor.market-structure-entities"
mutation_owner = "none_current"
transport_owner = "none_current"
persistence_owner = "none_current"
recovery_owner = "none_current"
projection_owner = "none_current"
policy_owner = "operator.markeitect"
implementation_state = "implemented"
composition_policy = "conditional"
temporal_status = "current"
acceptance_state = "accepted"
evidence_certainty = "verified_source"
configuration_path = "metrics.entity_analysis.enabled"
failure_isolation = "Nautilus actor callback boundary; failure is surfaced according to component policy."
cardinality = "one when composed"
style = "style.component.disabled"
profile_states = [{ profile = "profile.v3-es-minimal", enablement = "disabled" }]
evidence = ["evidence.composition"]
limitations = []

[[components]]
id = "actor.historical-planner"
label = "Historical Evidence Planner"
kind = "markeitech_actor"
logical_area = "data processing and state"
boundary = "boundary.intelligence"
implementation_ref = "src/markeitech/system/historical_planner.py:HistoricalEvidencePlannerActor"
composition_key = "historical_evidence_planner"
composition_order = 11
actor_id = "HISTORICAL-EVIDENCE-PLANNER"
responsibilities = ["Resolve semantic historical evidence demands into exact UTC request plans.", "Use immutable canonical-calendar projections for calendar-relative windows.", "Publish exact plans without calling providers or owning provider pacing."]
semantic_owner = "actor.historical-planner"
mutation_owner = "none_current"
transport_owner = "none_current"
persistence_owner = "none_current"
recovery_owner = "actor.historical-planner"
projection_owner = "none_current"
policy_owner = "operator.markeitect"
implementation_state = "implemented"
composition_policy = "always"
temporal_status = "current"
acceptance_state = "bounded_evidence"
evidence_certainty = "verified_source"
failure_isolation = "Demand rejection, calendar-window deferral, and bounded projection-delivery failure remain local to the planner actor."
cardinality = "one when composed"
style = "style.component.current"
profile_states = [{ profile = "profile.v3-es-minimal", enablement = "enabled" }]
evidence = ["evidence.composition", "evidence.historical-planner", "evidence.calendar-contracts"]
limitations = ["Recent-completed count windows use completed interval boundaries; they do not guarantee a requested number of traded observations across closures or provider gaps."]

[[components]]
id = "actor.watchlist"
label = "Watchlist"
kind = "markeitech_actor"
logical_area = "data acquisition"
boundary = "boundary.acquisition"
implementation_ref = "src/markeitech/system/watchlist.py:WatchlistActor"
composition_key = "watchlist"
composition_order = 12
actor_id = "WATCHLIST"
responsibilities = ["Watchlist responsibilities and lifecycle"]
semantic_owner = "actor.watchlist"
mutation_owner = "none_current"
transport_owner = "none_current"
persistence_owner = "none_current"
recovery_owner = "none_current"
projection_owner = "none_current"
policy_owner = "operator.markeitect"
implementation_state = "implemented"
composition_policy = "always"
temporal_status = "current"
acceptance_state = "accepted"
evidence_certainty = "verified_source"
failure_isolation = "Nautilus actor callback boundary; failure is surfaced according to component policy."
cardinality = "one when composed"
style = "style.component.current"
profile_states = [{ profile = "profile.v3-es-minimal", enablement = "enabled" }]
evidence = ["evidence.composition"]
limitations = []

[[components]]
id = "actor.data-acquisition"
label = "Data Acquisition"
kind = "markeitech_actor"
logical_area = "data acquisition"
boundary = "boundary.acquisition"
implementation_ref = "src/markeitech/system/acquisition.py:DataAcquisitionActor"
composition_key = "data_acquisition"
composition_order = 13
actor_id = "DATA-ACQUISITION"
responsibilities = ["Own provider-facing live demand reconciliation, subscription lifetime, historical queueing, pacing, retries, and execution.", "Execute exact HistoricalRequestPlan values without deciding sessions or calendar-relative request bounds.", "Publish acquisition lifecycle, historical batches, and consumer readiness."]
semantic_owner = "actor.data-acquisition"
mutation_owner = "none_current"
transport_owner = "none_current"
persistence_owner = "none_current"
recovery_owner = "none_current"
projection_owner = "none_current"
policy_owner = "operator.markeitect"
implementation_state = "implemented"
composition_policy = "always"
temporal_status = "current"
acceptance_state = "accepted"
evidence_certainty = "verified_source"
failure_isolation = "Nautilus actor callback boundary; failure is surfaced according to component policy."
cardinality = "one when composed"
style = "style.component.current"
profile_states = [{ profile = "profile.v3-es-minimal", enablement = "enabled" }]
evidence = ["evidence.composition", "evidence.historical-contracts"]
limitations = ["The verified local historical port has one active lane and no provider cancellation mapping."]

[[components]]
id = "actor.historical-probe"
label = "Historical Dependency Probe"
kind = "markeitech_actor"
logical_area = "data acquisition"
boundary = "boundary.acquisition"
implementation_ref = "src/markeitech/system/historical_probe.py:HistoricalDependencyProbeActor"
composition_key = "historical_dependency_probe:*"
composition_order = 15
actor_id = "HISTORICAL-DEPENDENCY-PROBE-*"
responsibilities = ["Historical Dependency Probe responsibilities and lifecycle"]
semantic_owner = "actor.historical-probe"
mutation_owner = "none_current"
transport_owner = "none_current"
persistence_owner = "none_current"
recovery_owner = "none_current"
projection_owner = "none_current"
policy_owner = "operator.markeitect"
implementation_state = "implemented"
composition_policy = "conditional"
temporal_status = "current"
acceptance_state = "accepted"
evidence_certainty = "verified_source"
configuration_path = "historical.probe.enabled"
failure_isolation = "Nautilus actor callback boundary; failure is surfaced according to component policy."
cardinality = "zero to many, configuration-bounded"
style = "style.component.disabled"
profile_states = []
evidence = ["evidence.composition"]
limitations = ["Direct-mode composition depends on both historical.probe.enabled and historical.probe.mode=direct; the mechanical profile census supports one Boolean path only. The tracked current_state_gated profile does not compose this legacy direct probe."]

[[components]]
id = "actor.current-state-historical-probe"
label = "Current-State Historical Demand Probe"
kind = "markeitech_actor"
logical_area = "data acquisition"
boundary = "boundary.acquisition"
implementation_ref = "src/markeitech/system/historical_probe.py:CurrentStateHistoricalDemandProbeActor"
composition_key = "current_state_historical_probe"
composition_order = 14
actor_id = "CURRENT-STATE-HISTORICAL-PROBE"
responsibilities = ["Gate one acceptance-only historical demand on reconciled current session state.", "Observe the correlated plan, readiness, and transient historical batches.", "Exercise one bounded snapshot retry when explicitly configured."]
semantic_owner = "none_current"
mutation_owner = "none_current"
transport_owner = "none_current"
persistence_owner = "none_current"
recovery_owner = "actor.current-state-historical-probe"
projection_owner = "none_current"
policy_owner = "operator.markeitect"
implementation_state = "implemented"
composition_policy = "conditional"
temporal_status = "current"
acceptance_state = "bounded_evidence"
evidence_certainty = "verified_source"
failure_isolation = "Nautilus actor callback boundary; terminal stop and bounded consumer-owned retry prevent late mutation or timer rearm."
cardinality = "zero or one when explicitly composed"
style = "style.component.current"
configuration_path = "historical.probe.enabled"
profile_states = [{ profile = "profile.v3-es-minimal", enablement = "enabled" }]
evidence = ["evidence.composition", "evidence.historical-contracts"]
limitations = ["Composition additionally requires historical.probe.mode=current_state_gated.", "Temporarily enabled by Markeitect for bounded checks; one connected 5/5 READY result does not create production analytical or provider authority."]

[[components]]
id = "actor.native-consumer-probe"
label = "Native Consumer Probe"
kind = "markeitech_actor"
logical_area = "system and infrastructure"
boundary = "boundary.system"
implementation_ref = "src/markeitech/system/native_consumer_probe.py:NativeConsumerProbeActor"
composition_key = "native_consumer_probe"
composition_order = 16
actor_id = "NATIVE-CONSUMER-PROBE"
responsibilities = ["Native Consumer Probe responsibilities and lifecycle"]
semantic_owner = "actor.native-consumer-probe"
mutation_owner = "none_current"
transport_owner = "none_current"
persistence_owner = "none_current"
recovery_owner = "none_current"
projection_owner = "none_current"
policy_owner = "operator.markeitect"
implementation_state = "implemented"
composition_policy = "conditional"
temporal_status = "current"
acceptance_state = "bounded_evidence"
evidence_certainty = "verified_source"
configuration_path = "acquisition.native_consumer_probe_enabled"
failure_isolation = "Nautilus actor callback boundary; failure is surfaced according to component policy."
cardinality = "one when composed"
style = "style.component.disabled"
profile_states = [{ profile = "profile.v3-es-minimal", enablement = "disabled" }]
evidence = ["evidence.composition"]
limitations = ["Completed diagnostic; incompatible with the active bar-only profile if enabled."]

[[components]]
id = "actor.runtime-resources"
label = "Runtime Resources"
kind = "markeitech_actor"
logical_area = "system and infrastructure"
boundary = "boundary.system"
implementation_ref = "src/markeitech/system/resource_actor.py:RuntimeResourceActor"
composition_key = "runtime_resources"
composition_order = 17
actor_id = "RUNTIME-RESOURCES"
responsibilities = ["Runtime Resources responsibilities and lifecycle"]
semantic_owner = "actor.runtime-resources"
mutation_owner = "none_current"
transport_owner = "none_current"
persistence_owner = "none_current"
recovery_owner = "none_current"
projection_owner = "none_current"
policy_owner = "operator.markeitect"
implementation_state = "implemented"
composition_policy = "conditional"
temporal_status = "current"
acceptance_state = "accepted"
evidence_certainty = "verified_source"
configuration_path = "runtime_resources.enabled"
failure_isolation = "Nautilus actor callback boundary; failure is surfaced according to component policy."
cardinality = "one when composed"
style = "style.component.disabled"
profile_states = [{ profile = "profile.v3-es-minimal", enablement = "disabled" }]
evidence = ["evidence.composition"]
limitations = []

[[components]]
id = "actor.runtime-resource-health"
label = "Runtime Resource Health"
kind = "markeitech_actor"
logical_area = "system and infrastructure"
boundary = "boundary.system"
implementation_ref = "src/markeitech/system/resource_health_actor.py:RuntimeResourceHealthActor"
composition_key = "runtime_resource_health"
composition_order = 18
actor_id = "RUNTIME-RESOURCE-HEALTH"
responsibilities = ["Runtime Resource Health responsibilities and lifecycle"]
semantic_owner = "actor.runtime-resource-health"
mutation_owner = "none_current"
transport_owner = "none_current"
persistence_owner = "none_current"
recovery_owner = "none_current"
projection_owner = "none_current"
policy_owner = "operator.markeitect"
implementation_state = "implemented"
composition_policy = "conditional"
temporal_status = "current"
acceptance_state = "unaccepted"
evidence_certainty = "verified_source"
configuration_path = "runtime_resources.health.enabled"
failure_isolation = "Nautilus actor callback boundary; failure is surfaced according to component policy."
cardinality = "one when composed"
style = "style.component.disabled"
profile_states = [{ profile = "profile.v3-es-minimal", enablement = "disabled" }]
evidence = ["evidence.resource-contracts"]
limitations = ["Effective composition also depends on runtime_resources.enabled."]

[[components]]
id = "actor.operational-persistence"
label = "Operational Persistence"
kind = "markeitech_actor"
logical_area = "system and infrastructure"
boundary = "boundary.system"
implementation_ref = "src/markeitech/system/persistence.py:OperationalPersistenceActor"
composition_key = "operational_persistence"
composition_order = 19
actor_id = "OPERATIONAL-PERSISTENCE"
responsibilities = ["Operational Persistence responsibilities and lifecycle"]
semantic_owner = "actor.operational-persistence"
mutation_owner = "none_current"
transport_owner = "none_current"
persistence_owner = "none_current"
recovery_owner = "none_current"
projection_owner = "none_current"
policy_owner = "operator.markeitect"
implementation_state = "implemented"
composition_policy = "always"
temporal_status = "current"
acceptance_state = "bounded_evidence"
evidence_certainty = "verified_source"
failure_isolation = "Nautilus actor callback boundary; failure is surfaced according to component policy."
cardinality = "one when composed"
style = "style.component.current"
profile_states = [{ profile = "profile.v3-es-minimal", enablement = "enabled" }]
evidence = ["evidence.persistence"]
limitations = ["Application-level persistence readiness is implemented; current-status prose still contains a conflicting historical statement."]

[[components]]
id = "component.live-node"
label = "Nautilus LiveNode"
kind = "framework"
logical_area = "system and infrastructure"
boundary = "boundary.nautilus"
responsibilities = ["Own native engines, actor composition, lifecycle, and data-client registration."]
semantic_owner = "component.live-node"
mutation_owner = "none_current"
transport_owner = "none_current"
persistence_owner = "none_current"
recovery_owner = "none_current"
projection_owner = "none_current"
policy_owner = "operator.markeitect"
implementation_state = "implemented"
composition_policy = "always"
temporal_status = "current"
acceptance_state = "not_applicable"
evidence_certainty = "verified_source"
failure_isolation = "Boundary-specific; no additional recovery guarantee is inferred."
cardinality = "one logical boundary"
style = "style.component.current"
profile_states = [{ profile = "profile.v3-es-minimal", enablement = "enabled" }]
evidence = ["evidence.node"]
limitations = []

[[components]]
id = "component.data-engine"
label = "Nautilus Data Engine"
kind = "engine"
logical_area = "system and infrastructure"
boundary = "boundary.nautilus"
responsibilities = ["Own native data request, subscription, cache, and callback routing."]
semantic_owner = "component.data-engine"
mutation_owner = "none_current"
transport_owner = "none_current"
persistence_owner = "none_current"
recovery_owner = "none_current"
projection_owner = "none_current"
policy_owner = "operator.markeitect"
implementation_state = "implemented"
composition_policy = "always"
temporal_status = "current"
acceptance_state = "not_applicable"
evidence_certainty = "verified_source"
failure_isolation = "Boundary-specific; no additional recovery guarantee is inferred."
cardinality = "one logical boundary"
style = "style.component.current"
profile_states = [{ profile = "profile.v3-es-minimal", enablement = "enabled" }]
evidence = ["evidence.node"]
limitations = []

[[components]]
id = "component.cache"
label = "Nautilus Cache"
kind = "engine"
logical_area = "system and infrastructure"
boundary = "boundary.nautilus"
responsibilities = ["Own native in-process cached runtime state."]
semantic_owner = "component.cache"
mutation_owner = "none_current"
transport_owner = "none_current"
persistence_owner = "none_current"
recovery_owner = "none_current"
projection_owner = "none_current"
policy_owner = "operator.markeitect"
implementation_state = "implemented"
composition_policy = "always"
temporal_status = "current"
acceptance_state = "not_applicable"
evidence_certainty = "verified_source"
failure_isolation = "Boundary-specific; no additional recovery guarantee is inferred."
cardinality = "one logical boundary"
style = "style.component.current"
profile_states = [{ profile = "profile.v3-es-minimal", enablement = "enabled" }]
evidence = ["evidence.node"]
limitations = ["Cache contents are not architecture evidence."]

[[components]]
id = "provider.interactive-brokers"
label = "Interactive Brokers / TWS / Gateway"
kind = "provider"
logical_area = "data acquisition"
boundary = "boundary.providers"
responsibilities = ["External market-data capability boundary."]
semantic_owner = "provider.interactive-brokers"
mutation_owner = "none_current"
transport_owner = "none_current"
persistence_owner = "none_current"
recovery_owner = "none_current"
projection_owner = "none_current"
policy_owner = "operator.markeitect"
implementation_state = "external"
composition_policy = "external"
temporal_status = "current"
acceptance_state = "not_applicable"
evidence_certainty = "verified_source"
failure_isolation = "Boundary-specific; no additional recovery guarantee is inferred."
cardinality = "one logical boundary"
style = "style.component.external"
profile_states = [{ profile = "profile.v3-es-minimal", enablement = "not_applicable" }]
evidence = ["evidence.node"]
limitations = ["Exact IB contract qualification, entitlement, returned data mode, request methods, callbacks, and cancellation mapping remain unknown."]

[[components]]
id = "queue.persistence"
label = "Persistence Admission Queue"
kind = "queue"
logical_area = "system and infrastructure"
boundary = "boundary.workers"
responsibilities = ["Bound accepted operational events before worker delivery."]
semantic_owner = "queue.persistence"
mutation_owner = "none_current"
transport_owner = "none_current"
persistence_owner = "none_current"
recovery_owner = "none_current"
projection_owner = "none_current"
policy_owner = "operator.markeitect"
implementation_state = "implemented"
composition_policy = "not_composed"
temporal_status = "current"
acceptance_state = "not_applicable"
evidence_certainty = "verified_source"
failure_isolation = "Boundary-specific; no additional recovery guarantee is inferred."
cardinality = "one logical boundary"
style = "style.component.current"
profile_states = [{ profile = "profile.v3-es-minimal", enablement = "enabled" }]
evidence = ["evidence.persistence"]
limitations = []

[[components]]
id = "worker.persistence"
label = "Persistence Writer Worker"
kind = "worker"
logical_area = "system and infrastructure"
boundary = "boundary.workers"
responsibilities = ["Batch and retry accepted operational-event writes off the actor callback path."]
semantic_owner = "worker.persistence"
mutation_owner = "none_current"
transport_owner = "none_current"
persistence_owner = "none_current"
recovery_owner = "none_current"
projection_owner = "none_current"
policy_owner = "operator.markeitect"
implementation_state = "implemented"
composition_policy = "not_composed"
temporal_status = "current"
acceptance_state = "not_applicable"
evidence_certainty = "verified_source"
failure_isolation = "Boundary-specific; no additional recovery guarantee is inferred."
cardinality = "one logical boundary"
style = "style.component.current"
profile_states = [{ profile = "profile.v3-es-minimal", enablement = "enabled" }]
evidence = ["evidence.persistence"]
limitations = []

[[components]]
id = "store.postgres"
label = "PostgreSQL Operational Audit"
kind = "data_store"
logical_area = "system and infrastructure"
boundary = "boundary.persistence"
responsibilities = ["Durable external sink for approved operational facts."]
semantic_owner = "store.postgres"
mutation_owner = "none_current"
transport_owner = "none_current"
persistence_owner = "none_current"
recovery_owner = "none_current"
projection_owner = "none_current"
policy_owner = "operator.markeitect"
implementation_state = "external"
composition_policy = "external"
temporal_status = "current"
acceptance_state = "not_applicable"
evidence_certainty = "verified_source"
failure_isolation = "Boundary-specific; no additional recovery guarantee is inferred."
cardinality = "one logical boundary"
style = "style.component.external"
profile_states = [{ profile = "profile.v3-es-minimal", enablement = "not_applicable" }]
evidence = ["evidence.persistence"]
limitations = ["Database-internal schema, transaction, index, privilege, recovery, and deployment topology are intentionally unknown in this diagram."]

[[components]]
id = "queue.discord"
label = "Discord Delivery Queue"
kind = "queue"
logical_area = "actions and projections"
boundary = "boundary.workers"
responsibilities = ["Bound optional outbound health notifications."]
semantic_owner = "queue.discord"
mutation_owner = "none_current"
transport_owner = "none_current"
persistence_owner = "none_current"
recovery_owner = "none_current"
projection_owner = "none_current"
policy_owner = "operator.markeitect"
implementation_state = "implemented"
composition_policy = "not_composed"
temporal_status = "current"
acceptance_state = "not_applicable"
evidence_certainty = "verified_source"
failure_isolation = "Boundary-specific; no additional recovery guarantee is inferred."
cardinality = "one logical boundary"
style = "style.component.current"
profile_states = [{ profile = "profile.v3-es-minimal", enablement = "disabled" }]
evidence = ["evidence.discord"]
limitations = []

[[components]]
id = "worker.discord"
label = "Discord Delivery Worker"
kind = "worker"
logical_area = "actions and projections"
boundary = "boundary.workers"
responsibilities = ["Deliver optional notifications outside actor callbacks."]
semantic_owner = "worker.discord"
mutation_owner = "none_current"
transport_owner = "none_current"
persistence_owner = "none_current"
recovery_owner = "none_current"
projection_owner = "none_current"
policy_owner = "operator.markeitect"
implementation_state = "implemented"
composition_policy = "not_composed"
temporal_status = "current"
acceptance_state = "not_applicable"
evidence_certainty = "verified_source"
failure_isolation = "Boundary-specific; no additional recovery guarantee is inferred."
cardinality = "one logical boundary"
style = "style.component.current"
profile_states = [{ profile = "profile.v3-es-minimal", enablement = "disabled" }]
evidence = ["evidence.discord"]
limitations = []

[[components]]
id = "projection.discord"
label = "Discord"
kind = "projection"
logical_area = "actions and projections"
boundary = "boundary.projections"
responsibilities = ["External notification projection; never canonical truth."]
semantic_owner = "projection.discord"
mutation_owner = "none_current"
transport_owner = "none_current"
persistence_owner = "none_current"
recovery_owner = "none_current"
projection_owner = "none_current"
policy_owner = "operator.markeitect"
implementation_state = "external"
composition_policy = "external"
temporal_status = "current"
acceptance_state = "not_applicable"
evidence_certainty = "verified_source"
failure_isolation = "Boundary-specific; no additional recovery guarantee is inferred."
cardinality = "one logical boundary"
style = "style.component.external"
profile_states = [{ profile = "profile.v3-es-minimal", enablement = "not_applicable" }]
evidence = ["evidence.discord"]
limitations = []

[[components]]
id = "projection.visual-files"
label = "Visual Debug Files"
kind = "projection"
logical_area = "actions and projections"
boundary = "boundary.projections"
responsibilities = ["Filesystem diagnostic projection; generated runtime capture is not architecture authority."]
semantic_owner = "projection.visual-files"
mutation_owner = "none_current"
transport_owner = "none_current"
persistence_owner = "none_current"
recovery_owner = "none_current"
projection_owner = "none_current"
policy_owner = "operator.markeitect"
implementation_state = "external"
composition_policy = "external"
temporal_status = "current"
acceptance_state = "not_applicable"
evidence_certainty = "verified_source"
failure_isolation = "Boundary-specific; no additional recovery guarantee is inferred."
cardinality = "one logical boundary"
style = "style.component.external"
profile_states = [{ profile = "profile.v3-es-minimal", enablement = "not_applicable" }]
evidence = ["evidence.visual-debug"]
limitations = []

[[components]]
id = "operator.markeitect"
label = "Markeitect / Operator"
kind = "operator"
logical_area = "actions and projections"
boundary = "boundary.projections"
responsibilities = ["Final architecture, review, operational, merge, and release authority."]
semantic_owner = "operator.markeitect"
mutation_owner = "none_current"
transport_owner = "none_current"
persistence_owner = "none_current"
recovery_owner = "none_current"
projection_owner = "none_current"
policy_owner = "operator.markeitect"
implementation_state = "external"
composition_policy = "external"
temporal_status = "current"
acceptance_state = "not_applicable"
evidence_certainty = "verified_source"
failure_isolation = "Boundary-specific; no additional recovery guarantee is inferred."
cardinality = "one logical boundary"
style = "style.component.external"
profile_states = [{ profile = "profile.v3-es-minimal", enablement = "not_applicable" }]
evidence = ["evidence.current-status"]
limitations = []

[[components]]
id = "future.semantic-events"
label = "Semantic Events"
kind = "future_component"
logical_area = "future governed boundaries"
boundary = "boundary.future"
responsibilities = ["Future typed derived-event boundary."]
semantic_owner = "future.semantic-events"
mutation_owner = "none_current"
transport_owner = "none_current"
persistence_owner = "none_current"
recovery_owner = "none_current"
projection_owner = "none_current"
policy_owner = "operator.markeitect"
implementation_state = "future"
composition_policy = "not_composed"
temporal_status = "future"
acceptance_state = "not_applicable"
evidence_certainty = "recommendation"
failure_isolation = "Boundary-specific; no additional recovery guarantee is inferred."
cardinality = "one logical boundary"
style = "style.component.future"
profile_states = [{ profile = "profile.v3-es-minimal", enablement = "not_applicable" }]
evidence = ["evidence.roadmap"]
limitations = ["Not implemented or composed."]

[[components]]
id = "future.opportunities"
label = "Plural Opportunities"
kind = "future_component"
logical_area = "future governed boundaries"
boundary = "boundary.future"
responsibilities = ["Future plural opportunity lifecycle."]
semantic_owner = "future.opportunities"
mutation_owner = "none_current"
transport_owner = "none_current"
persistence_owner = "none_current"
recovery_owner = "none_current"
projection_owner = "none_current"
policy_owner = "operator.markeitect"
implementation_state = "future"
composition_policy = "not_composed"
temporal_status = "future"
acceptance_state = "not_applicable"
evidence_certainty = "recommendation"
failure_isolation = "Boundary-specific; no additional recovery guarantee is inferred."
cardinality = "one logical boundary"
style = "style.component.future"
profile_states = [{ profile = "profile.v3-es-minimal", enablement = "not_applicable" }]
evidence = ["evidence.roadmap"]
limitations = ["Not implemented or composed."]

[[components]]
id = "future.sir-loke"
label = "Sir Loke Advisory Agent"
kind = "future_component"
logical_area = "future governed boundaries"
boundary = "boundary.future"
responsibilities = ["Future governed read-only advisory consumer."]
semantic_owner = "future.sir-loke"
mutation_owner = "none_current"
transport_owner = "none_current"
persistence_owner = "none_current"
recovery_owner = "none_current"
projection_owner = "none_current"
policy_owner = "operator.markeitect"
implementation_state = "future"
composition_policy = "not_composed"
temporal_status = "future"
acceptance_state = "not_applicable"
evidence_certainty = "recommendation"
failure_isolation = "Boundary-specific; no additional recovery guarantee is inferred."
cardinality = "one logical boundary"
style = "style.component.future"
profile_states = [{ profile = "profile.v3-es-minimal", enablement = "not_applicable" }]
evidence = ["evidence.roadmap"]
limitations = ["Kite advisors are development-time collaborators and are unrelated to Sir Loke runtime behavior."]

[[components]]
id = "future.controlled-execution"
label = "Future Controlled Execution Boundary"
kind = "future_component"
logical_area = "future governed boundaries"
boundary = "boundary.future"
responsibilities = ["Future separately governed execution boundary."]
semantic_owner = "future.controlled-execution"
mutation_owner = "none_current"
transport_owner = "none_current"
persistence_owner = "none_current"
recovery_owner = "none_current"
projection_owner = "none_current"
policy_owner = "operator.markeitect"
implementation_state = "future"
composition_policy = "not_composed"
temporal_status = "future"
acceptance_state = "not_applicable"
evidence_certainty = "recommendation"
failure_isolation = "Boundary-specific; no additional recovery guarantee is inferred."
cardinality = "one logical boundary"
style = "style.component.future"
profile_states = [{ profile = "profile.v3-es-minimal", enablement = "not_applicable" }]
evidence = ["evidence.roadmap"]
limitations = ["No current order submission or execution exists; no authority is granted by this diagram."]

[[capabilities]]
id = "capability.acquisition.watchlist-last"
component = "actor.data-acquisition"
label = "Watchlist last-price bar acquisition"
implementation_state = "implemented"
composition_policy = "conditional"
configuration_path = "watchlist.members[].capabilities contains watchlist_last"
profile = "profile.v3-es-minimal"
enablement = "enabled"
evidence = ["evidence.profile"]
limitations = ["Exact provider method and entitlement remain unknown."]

[[capabilities]]
id = "capability.acquisition.historical-bars"
component = "actor.data-acquisition"
label = "Bounded analytical historical bar requests"
implementation_state = "implemented"
composition_policy = "conditional"
configuration_path = "historical plus consumer AnalyticalDemand"
profile = "profile.v3-es-minimal"
enablement = "enabled"
evidence = ["evidence.historical-contracts"]
limitations = ["Enabled by declared consumer demand, not by a single Boolean flag."]

[[capabilities]]
id = "capability.session-metrics.completed-bars"
component = "actor.session-metrics"
label = "Completed-bar foundation"
implementation_state = "implemented"
composition_policy = "conditional"
configuration_path = "metrics.session_measurements.enabled"
profile = "profile.v3-es-minimal"
enablement = "disabled"
evidence = ["evidence.profile"]
limitations = []

[[capabilities]]
id = "capability.session-metrics.rolling"
component = "actor.session-metrics"
label = "Rolling measurements"
implementation_state = "implemented"
composition_policy = "conditional"
configuration_path = "metrics.session_measurements.rolling_measurements.enabled"
profile = "profile.v3-es-minimal"
enablement = "disabled"
evidence = ["evidence.profile"]
limitations = []

[[capabilities]]
id = "capability.session-metrics.session-references"
component = "actor.session-metrics"
label = "Session reference measurements"
implementation_state = "implemented"
composition_policy = "conditional"
configuration_path = "metrics.session_measurements.session_references.enabled"
profile = "profile.v3-es-minimal"
enablement = "disabled"
evidence = ["evidence.profile"]
limitations = []

[[capabilities]]
id = "capability.session-metrics.session-windows"
component = "actor.session-metrics"
label = "Calendar-relative session windows"
implementation_state = "implemented"
composition_policy = "conditional"
configuration_path = "metrics.session_measurements.session_windows.enabled"
profile = "profile.v3-es-minimal"
enablement = "disabled"
evidence = ["evidence.profile"]
limitations = []

[[capabilities]]
id = "capability.visual-debug.capture"
component = "actor.visual-debug"
label = "Passive visual debug capture"
implementation_state = "implemented"
composition_policy = "conditional"
configuration_path = "visual_debug_capture.enabled"
profile = "profile.v3-es-minimal"
enablement = "disabled"
evidence = ["evidence.visual-debug"]
limitations = ["Diagnostic projection is not architecture or analytical authority."]

[[capabilities]]
id = "capability.discord.notifications"
component = "actor.discord-health"
label = "Queued Discord health notifications"
implementation_state = "implemented"
composition_policy = "conditional"
configuration_path = "discord.enabled"
profile = "profile.v3-es-minimal"
enablement = "disabled"
evidence = ["evidence.discord"]
limitations = []

[[capabilities]]
id = "capability.runtime-resources.telemetry"
component = "actor.runtime-resources"
label = "Runtime resource telemetry"
implementation_state = "implemented"
composition_policy = "conditional"
configuration_path = "runtime_resources.enabled"
profile = "profile.v3-es-minimal"
enablement = "disabled"
evidence = ["evidence.resource-contracts"]
limitations = []

[[capabilities]]
id = "capability.runtime-resources.health"
component = "actor.runtime-resource-health"
label = "Runtime resource health classification"
implementation_state = "implemented"
composition_policy = "conditional"
configuration_path = "runtime_resources.enabled and runtime_resources.health.enabled"
profile = "profile.v3-es-minimal"
enablement = "disabled"
evidence = ["evidence.resource-contracts"]
limitations = []

[[contracts]]
id = "contract.system-health"
label = "System health"
transport_kind = "nautilus_signal"
type_name = "markeitech.system.health"
schema_version_kind = "explicit contract identity or framework-native type"
canonical_owner = "actor.system-control"
producers = ["actor.system-control"]
consumers = ["actor.operational-persistence", "actor.discord-health"]
parent_contracts = []
identity_fields = ["contract identity", "source identity", "instrument where applicable"]
join_key = []
expected_cardinality = "contract-specific and configuration-bounded"
event_clock = "source-defined or not applicable"
availability_clock = "local receipt or publication time where represented"
clock_unit = "UTC nanoseconds where the contract carries instants"
source_classes = ["verified_source"]
reconciliation_policy = "contract owner preserves duplicates, conflicts, revisions, and unknowns according to source contract"
retention = "transient unless explicitly persisted as an operational fact"
evidence = ["evidence.system-contracts"]
limitations = []

[[contracts]]
id = "contract.component-failure"
label = "Component failure"
transport_kind = "nautilus_signal"
type_name = "markeitech.component.failure"
schema_version_kind = "explicit contract identity or framework-native type"
canonical_owner = "actor.system-control"
producers = ["actor.operational-persistence", "actor.discord-health", "actor.visual-debug"]
consumers = ["actor.system-control"]
parent_contracts = []
identity_fields = ["contract identity", "source identity", "instrument where applicable"]
join_key = []
expected_cardinality = "contract-specific and configuration-bounded"
event_clock = "source-defined or not applicable"
availability_clock = "local receipt or publication time where represented"
clock_unit = "UTC nanoseconds where the contract carries instants"
source_classes = ["verified_source"]
reconciliation_policy = "contract owner preserves duplicates, conflicts, revisions, and unknowns according to source contract"
retention = "transient unless explicitly persisted as an operational fact"
evidence = ["evidence.system-contracts"]
limitations = []

[[contracts]]
id = "contract.persistence-ready-request"
label = "Persistence readiness request"
transport_kind = "nautilus_signal"
type_name = "markeitech.persistence.ready.request"
schema_version_kind = "explicit contract identity or framework-native type"
canonical_owner = "actor.system-control"
producers = ["actor.system-control"]
consumers = ["actor.operational-persistence"]
parent_contracts = []
identity_fields = ["contract identity", "source identity", "instrument where applicable"]
join_key = []
expected_cardinality = "contract-specific and configuration-bounded"
event_clock = "source-defined or not applicable"
availability_clock = "local receipt or publication time where represented"
clock_unit = "UTC nanoseconds where the contract carries instants"
source_classes = ["verified_source"]
reconciliation_policy = "contract owner preserves duplicates, conflicts, revisions, and unknowns according to source contract"
retention = "transient unless explicitly persisted as an operational fact"
evidence = ["evidence.system-contracts"]
limitations = []

[[contracts]]
id = "contract.persistence-ready"
label = "Persistence readiness"
transport_kind = "nautilus_signal"
type_name = "markeitech.persistence.ready"
schema_version_kind = "explicit contract identity or framework-native type"
canonical_owner = "actor.operational-persistence"
producers = ["actor.operational-persistence"]
consumers = ["actor.system-control"]
parent_contracts = []
identity_fields = ["contract identity", "source identity", "instrument where applicable"]
join_key = []
expected_cardinality = "contract-specific and configuration-bounded"
event_clock = "source-defined or not applicable"
availability_clock = "local receipt or publication time where represented"
clock_unit = "UTC nanoseconds where the contract carries instants"
source_classes = ["verified_source"]
reconciliation_policy = "contract owner preserves duplicates, conflicts, revisions, and unknowns according to source contract"
retention = "transient unless explicitly persisted as an operational fact"
evidence = ["evidence.system-contracts"]
limitations = []

[[contracts]]
id = "contract.acquisition-status"
label = "Acquisition status"
transport_kind = "nautilus_signal"
type_name = "markeitech.acquisition.status"
schema_version_kind = "explicit contract identity or framework-native type"
canonical_owner = "actor.data-acquisition"
producers = ["actor.data-acquisition"]
consumers = ["actor.system-control", "actor.evidence-health", "actor.watchlist"]
parent_contracts = []
identity_fields = ["contract identity", "source identity", "instrument where applicable"]
join_key = []
expected_cardinality = "contract-specific and configuration-bounded"
event_clock = "source-defined or not applicable"
availability_clock = "local receipt or publication time where represented"
clock_unit = "UTC nanoseconds where the contract carries instants"
source_classes = ["verified_source"]
reconciliation_policy = "contract owner preserves duplicates, conflicts, revisions, and unknowns according to source contract"
retention = "transient unless explicitly persisted as an operational fact"
evidence = ["evidence.system-contracts"]
limitations = []

[[contracts]]
id = "contract.acquisition-status-request"
label = "Acquisition status request"
transport_kind = "nautilus_signal"
type_name = "markeitech.acquisition.status.request"
schema_version_kind = "explicit contract identity or framework-native type"
canonical_owner = "actor.watchlist"
producers = ["actor.watchlist"]
consumers = ["actor.data-acquisition"]
parent_contracts = []
identity_fields = ["contract identity", "source identity", "instrument where applicable"]
join_key = []
expected_cardinality = "contract-specific and configuration-bounded"
event_clock = "source-defined or not applicable"
availability_clock = "local receipt or publication time where represented"
clock_unit = "UTC nanoseconds where the contract carries instants"
source_classes = ["verified_source"]
reconciliation_policy = "contract owner preserves duplicates, conflicts, revisions, and unknowns according to source contract"
retention = "transient unless explicitly persisted as an operational fact"
evidence = ["evidence.system-contracts"]
limitations = []

[[contracts]]
id = "contract.acquisition-stream"
label = "Acquisition stream state"
transport_kind = "nautilus_signal"
type_name = "markeitech.acquisition.stream"
schema_version_kind = "explicit contract identity or framework-native type"
canonical_owner = "actor.data-acquisition"
producers = ["actor.data-acquisition"]
consumers = ["actor.evidence-health"]
parent_contracts = []
identity_fields = ["contract identity", "source identity", "instrument where applicable"]
join_key = []
expected_cardinality = "contract-specific and configuration-bounded"
event_clock = "source-defined or not applicable"
availability_clock = "local receipt or publication time where represented"
clock_unit = "UTC nanoseconds where the contract carries instants"
source_classes = ["verified_source"]
reconciliation_policy = "contract owner preserves duplicates, conflicts, revisions, and unknowns according to source contract"
retention = "transient unless explicitly persisted as an operational fact"
evidence = ["evidence.system-contracts"]
limitations = []

[[contracts]]
id = "contract.watchlist-demand"
label = "Watchlist demand"
transport_kind = "nautilus_signal"
type_name = "markeitech.watchlist.demand"
schema_version_kind = "explicit contract identity or framework-native type"
canonical_owner = "actor.watchlist"
producers = ["actor.watchlist"]
consumers = ["actor.data-acquisition"]
parent_contracts = []
identity_fields = ["contract identity", "source identity", "instrument where applicable"]
join_key = []
expected_cardinality = "contract-specific and configuration-bounded"
event_clock = "source-defined or not applicable"
availability_clock = "local receipt or publication time where represented"
clock_unit = "UTC nanoseconds where the contract carries instants"
source_classes = ["verified_source"]
reconciliation_policy = "contract owner preserves duplicates, conflicts, revisions, and unknowns according to source contract"
retention = "transient unless explicitly persisted as an operational fact"
evidence = ["evidence.system-contracts"]
limitations = []

[[contracts]]
id = "contract.analytical-demand"
label = "Analytical demand"
transport_kind = "nautilus_signal"
type_name = "markeitech.analytics.demand"
schema_version_kind = "explicit contract identity or framework-native type"
canonical_owner = "actor.session-metrics"
producers = ["actor.session-metrics"]
consumers = ["actor.data-acquisition"]
parent_contracts = []
identity_fields = ["contract identity", "source identity", "instrument where applicable"]
join_key = []
expected_cardinality = "contract-specific and configuration-bounded"
event_clock = "source-defined or not applicable"
availability_clock = "local receipt or publication time where represented"
clock_unit = "UTC nanoseconds where the contract carries instants"
source_classes = ["verified_source"]
reconciliation_policy = "contract owner preserves duplicates, conflicts, revisions, and unknowns according to source contract"
retention = "transient unless explicitly persisted as an operational fact"
evidence = ["evidence.system-contracts"]
limitations = []

[[contracts]]
id = "contract.watchlist-membership"
label = "Watchlist membership"
transport_kind = "nautilus_signal"
type_name = "markeitech.watchlist.membership"
schema_version_kind = "explicit contract identity or framework-native type"
canonical_owner = "actor.watchlist"
producers = ["actor.watchlist"]
consumers = ["actor.operational-persistence", "actor.discord-health"]
parent_contracts = []
identity_fields = ["contract identity", "source identity", "instrument where applicable"]
join_key = []
expected_cardinality = "contract-specific and configuration-bounded"
event_clock = "source-defined or not applicable"
availability_clock = "local receipt or publication time where represented"
clock_unit = "UTC nanoseconds where the contract carries instants"
source_classes = ["verified_source"]
reconciliation_policy = "contract owner preserves duplicates, conflicts, revisions, and unknowns according to source contract"
retention = "transient unless explicitly persisted as an operational fact"
evidence = ["evidence.system-contracts"]
limitations = []

[[contracts]]
id = "contract.watchlist-lifecycle"
label = "Watchlist lifecycle"
transport_kind = "nautilus_signal"
type_name = "markeitech.watchlist.lifecycle"
schema_version_kind = "explicit contract identity or framework-native type"
canonical_owner = "actor.watchlist"
producers = ["actor.watchlist"]
consumers = ["actor.operational-persistence", "actor.discord-health"]
parent_contracts = []
identity_fields = ["contract identity", "source identity", "instrument where applicable"]
join_key = []
expected_cardinality = "contract-specific and configuration-bounded"
event_clock = "source-defined or not applicable"
availability_clock = "local receipt or publication time where represented"
clock_unit = "UTC nanoseconds where the contract carries instants"
source_classes = ["verified_source"]
reconciliation_policy = "contract owner preserves duplicates, conflicts, revisions, and unknowns according to source contract"
retention = "transient unless explicitly persisted as an operational fact"
evidence = ["evidence.system-contracts"]
limitations = []

[[contracts]]
id = "contract.historical-demand"
label = "Historical dependency demand"
transport_kind = "nautilus_signal"
type_name = "markeitech.historical.dependency_demand"
schema_version_kind = "explicit contract identity or framework-native type"
canonical_owner = "actor.historical-planner"
producers = ["actor.session-metrics", "actor.historical-probe", "actor.current-state-historical-probe"]
consumers = ["actor.historical-planner"]
parent_contracts = []
identity_fields = ["contract identity", "source identity", "instrument where applicable"]
join_key = []
expected_cardinality = "contract-specific and configuration-bounded"
event_clock = "source-defined or not applicable"
availability_clock = "local receipt or publication time where represented"
clock_unit = "UTC nanoseconds where the contract carries instants"
source_classes = ["verified_source"]
reconciliation_policy = "contract owner preserves duplicates, conflicts, revisions, and unknowns according to source contract"
retention = "transient unless explicitly persisted as an operational fact"
evidence = ["evidence.historical-contracts"]
limitations = []

[[contracts]]
id = "contract.historical-request-plan"
label = "Exact historical request plan"
transport_kind = "nautilus_custom_data"
type_name = "markeitech.historical.request_plan.v1"
python_symbol = "HistoricalRequestPlan"
schema_version_kind = "versioned transport identity"
canonical_owner = "actor.historical-planner"
producers = ["actor.historical-planner"]
consumers = ["actor.data-acquisition", "actor.current-state-historical-probe"]
parent_contracts = ["contract.historical-demand"]
identity_fields = ["demand_id", "calendar_id", "calendar_definition_digest", "request.request_id"]
join_key = ["demand_id", "request.request_id"]
expected_cardinality = "one exact plan per accepted demand in the current compiler boundary"
event_clock = "request end and planner timestamp in UTC nanoseconds"
availability_clock = "planned_at_ns"
clock_unit = "UTC nanoseconds"
source_classes = ["verified_source"]
reconciliation_policy = "DataAcquisitionActor validates scope and resource limits, then queues the exact request without reinterpreting its calendar bounds."
retention = "transient custom data; operational lifecycle facts may be persisted separately"
evidence = ["evidence.historical-contracts", "evidence.historical-planner"]
limitations = ["Plan publication does not prove provider acceptance, delivery, completeness, or connected runtime success."]

[[contracts]]
id = "contract.historical-execution"
label = "Historical execution lifecycle"
transport_kind = "nautilus_signal"
type_name = "markeitech.historical.execution"
schema_version_kind = "explicit contract identity or framework-native type"
canonical_owner = "actor.data-acquisition"
producers = ["actor.data-acquisition"]
consumers = ["actor.session-metrics", "actor.historical-probe"]
parent_contracts = []
identity_fields = ["contract identity", "source identity", "instrument where applicable"]
join_key = []
expected_cardinality = "contract-specific and configuration-bounded"
event_clock = "source-defined or not applicable"
availability_clock = "local receipt or publication time where represented"
clock_unit = "UTC nanoseconds where the contract carries instants"
source_classes = ["verified_source"]
reconciliation_policy = "contract owner preserves duplicates, conflicts, revisions, and unknowns according to source contract"
retention = "transient unless explicitly persisted as an operational fact"
evidence = ["evidence.historical-contracts"]
limitations = []

[[contracts]]
id = "contract.historical-readiness"
label = "Historical readiness"
transport_kind = "nautilus_signal"
type_name = "markeitech.historical.readiness"
schema_version_kind = "explicit contract identity or framework-native type"
canonical_owner = "actor.data-acquisition"
producers = ["actor.data-acquisition"]
consumers = ["actor.session-metrics", "actor.historical-probe", "actor.current-state-historical-probe"]
parent_contracts = []
identity_fields = ["contract identity", "source identity", "instrument where applicable"]
join_key = []
expected_cardinality = "contract-specific and configuration-bounded"
event_clock = "source-defined or not applicable"
availability_clock = "local receipt or publication time where represented"
clock_unit = "UTC nanoseconds where the contract carries instants"
source_classes = ["verified_source"]
reconciliation_policy = "contract owner preserves duplicates, conflicts, revisions, and unknowns according to source contract"
retention = "transient unless explicitly persisted as an operational fact"
evidence = ["evidence.historical-contracts"]
limitations = []

[[contracts]]
id = "contract.historical-batch"
label = "Historical bar batch"
transport_kind = "nautilus_custom_data"
type_name = "MarkeitechHistoricalBatch"
schema_version_kind = "explicit contract identity or framework-native type"
canonical_owner = "actor.data-acquisition"
producers = ["actor.data-acquisition"]
consumers = ["actor.session-metrics", "actor.historical-probe", "actor.current-state-historical-probe"]
parent_contracts = []
identity_fields = ["contract identity", "source identity", "instrument where applicable"]
join_key = []
expected_cardinality = "contract-specific and configuration-bounded"
event_clock = "source-defined or not applicable"
availability_clock = "local receipt or publication time where represented"
clock_unit = "UTC nanoseconds where the contract carries instants"
source_classes = ["verified_source"]
reconciliation_policy = "contract owner preserves duplicates, conflicts, revisions, and unknowns according to source contract"
retention = "transient unless explicitly persisted as an operational fact"
evidence = ["evidence.historical-contracts"]
limitations = ["One local active historical lane; callback request identity and provider cancellation are not preserved by the verified local port."]

[[contracts]]
id = "contract.calendar-transition"
label = "Canonical calendar transition"
transport_kind = "nautilus_custom_data"
type_name = "markeitech.calendar.transition.v2"
python_symbol = "CalendarTransitionV2"
schema_version_kind = "versioned transport identity"
canonical_owner = "actor.session-state"
producers = ["actor.session-state"]
consumers = ["actor.evidence-health", "actor.historical-planner", "actor.current-state-historical-probe", "actor.operational-persistence"]
parent_contracts = []
identity_fields = ["event_id", "calendar_id", "definition_version", "definition_digest", "definition_effective_from_ns", "source", "source_epoch", "revision"]
join_key = ["calendar_id", "definition_digest", "source_epoch", "revision"]
expected_cardinality = "initial state and changed state per active calendar, configuration-bounded"
event_clock = "state_effective_from_ns and evaluated_as_of_ns"
availability_clock = "published_ts_ns"
clock_unit = "UTC nanoseconds"
source_classes = ["verified_source"]
reconciliation_policy = "Consumers accept only the configured calendar definition and current SESSION-STATE source epoch; conflicts remain explicit."
retention = "transient custom data; accepted transition facts enter bounded operational persistence"
evidence = ["evidence.calendar-contracts", "evidence.canonical-calendar"]
limitations = ["A transition is current calendar context, not a historical market observation or analytical conclusion."]

[[contracts]]
id = "contract.calendar-projection-request"
label = "Canonical calendar projection request"
transport_kind = "nautilus_custom_data"
type_name = "markeitech.calendar.projection.request.v1"
python_symbol = "CalendarProjectionRequest"
schema_version_kind = "versioned transport identity"
canonical_owner = "actor.session-state"
producers = ["actor.evidence-health", "actor.session-metrics", "actor.historical-planner"]
consumers = ["actor.session-state"]
parent_contracts = []
identity_fields = ["request_id", "requester", "calendar_ids", "start_ns", "end_ns"]
join_key = ["request_id", "requester"]
expected_cardinality = "one outstanding request per requester; retries receive new request IDs and remain configuration-bounded"
event_clock = "requested projection UTC bounds"
availability_clock = "requested_ts_ns"
clock_unit = "UTC nanoseconds"
source_classes = ["verified_source"]
reconciliation_policy = "SessionStateActor validates request bounds and calendar count before constructing each requested projection."
retention = "transient custom data"
evidence = ["evidence.calendar-contracts"]
limitations = ["Request publication has no replay guarantee and is not readiness acceptance."]

[[contracts]]
id = "contract.calendar-projection-response"
label = "Canonical calendar projection response"
transport_kind = "nautilus_custom_data"
type_name = "markeitech.calendar.projection.response.v2"
python_symbol = "CalendarProjectionResponse"
schema_version_kind = "versioned transport identity with typed per-calendar failure accounting"
canonical_owner = "actor.session-state"
producers = ["actor.session-state"]
consumers = ["actor.evidence-health", "actor.session-metrics", "actor.historical-planner"]
parent_contracts = ["contract.calendar-projection-request"]
identity_fields = ["request_id", "requester", "source", "source_epoch", "requested_calendar_ids"]
join_key = ["request_id", "requester"]
expected_cardinality = "one response accounts for every requested calendar exactly once as projection, unavailable, or failure"
event_clock = "projection coverage and generated_ts_ns"
availability_clock = "generated_ts_ns"
clock_unit = "UTC nanoseconds"
source_classes = ["verified_source"]
reconciliation_policy = "Consumers correlate exact request/source/epoch, verify definition digest and coverage, retain successful calendars, and retry only explicitly retryable unresolved outcomes within configured attempt and elapsed bounds."
retention = "transient custom data"
evidence = ["evidence.calendar-contracts", "evidence.canonical-calendar"]
limitations = ["No startup replay guarantee; ordinary construction failures are typed, but publication or BaseException failures are not converted into successful delivery."]

[[contracts]]
id = "contract.calendar-state-snapshot-request"
label = "Canonical calendar current-state snapshot request"
transport_kind = "nautilus_custom_data"
type_name = "markeitech.calendar.state.snapshot.request.v1"
python_symbol = "CalendarStateSnapshotRequest"
schema_version_kind = "versioned transport identity"
canonical_owner = "actor.session-state"
producers = ["actor.evidence-health", "actor.historical-planner", "actor.current-state-historical-probe"]
consumers = ["actor.session-state"]
parent_contracts = []
identity_fields = ["cycle_id", "request_id", "attempt", "requester", "expected_source", "expected_source_epoch", "calendar_expectations", "delivery_policy_version"]
join_key = ["cycle_id", "request_id", "requester", "attempt"]
expected_cardinality = "one bounded attempt at a time per synchronization cycle and exact requester"
event_clock = "requested_as_of_ns and requested_ts_ns"
availability_clock = "requested_ts_ns with deadline_ts_ns admission bound"
clock_unit = "UTC nanoseconds"
source_classes = ["verified_source"]
reconciliation_policy = "SessionStateActor admits only exact configured requesters, calendar definitions, attempt bounds, policy version, source epoch, and deadline."
retention = "transient request; producer duplicate cache is bounded and non-durable"
evidence = ["evidence.calendar-contracts", "evidence.composition"]
limitations = ["Local custom-data publication is transient and does not prove consumer observation."]

[[contracts]]
id = "contract.calendar-state-snapshot-response"
label = "Canonical calendar current-state snapshot response"
transport_kind = "nautilus_custom_data"
type_name = "markeitech.calendar.state.snapshot.response.v1"
python_symbol = "CalendarStateSnapshotResponse"
schema_version_kind = "versioned transport identity with typed complete per-calendar accounting"
canonical_owner = "actor.session-state"
producers = ["actor.session-state"]
consumers = ["actor.evidence-health", "actor.historical-planner", "actor.current-state-historical-probe"]
parent_contracts = ["contract.calendar-state-snapshot-request", "contract.calendar-transition"]
identity_fields = ["cycle_id", "request_id", "attempt", "requester", "source", "source_epoch", "requested_calendar_ids", "delivery_policy_version"]
join_key = ["cycle_id", "request_id", "requester", "attempt"]
expected_cardinality = "one response completely accounts for the exact requested calendar population as current state or typed failure"
event_clock = "one evaluated_as_of_ns owner cut plus exact per-state state_effective_from_ns and revision-evaluation lineage"
availability_clock = "request_received_ts_ns, generated_ts_ns, published_ts_ns, and deadline_ts_ns"
clock_unit = "UTC nanoseconds"
source_classes = ["verified_source"]
reconciliation_policy = "Consumers correlate exact request/source/run/definition/policy identity, install only gap-free watermarks, and retry within configured attempt and elapsed bounds."
retention = "transient response; no snapshot, retry, buffer, or watermark persistence"
evidence = ["evidence.calendar-contracts", "evidence.canonical-calendar"]
limitations = ["A response is point-in-time deterministic calendar state, not provider-observed exchange status or historical market evidence."]

[[contracts]]
id = "contract.evidence-health"
label = "Evidence health"
transport_kind = "nautilus_signal"
type_name = "markeitech.evidence.health"
schema_version_kind = "explicit contract identity or framework-native type"
canonical_owner = "actor.evidence-health"
producers = ["actor.evidence-health"]
consumers = ["actor.session-metrics", "future.sir-loke"]
parent_contracts = []
identity_fields = ["contract identity", "source identity", "instrument where applicable"]
join_key = []
expected_cardinality = "contract-specific and configuration-bounded"
event_clock = "source-defined or not applicable"
availability_clock = "local receipt or publication time where represented"
clock_unit = "UTC nanoseconds where the contract carries instants"
source_classes = ["verified_source"]
reconciliation_policy = "contract owner preserves duplicates, conflicts, revisions, and unknowns according to source contract"
retention = "transient unless explicitly persisted as an operational fact"
evidence = ["evidence.intelligence-contracts"]
limitations = []

[[contracts]]
id = "contract.evidence-health-snapshot-request"
label = "Evidence-health snapshot request"
transport_kind = "nautilus_signal"
type_name = "markeitech.evidence.health.snapshot.request"
schema_version_kind = "explicit contract identity or framework-native type"
canonical_owner = "actor.evidence-health"
producers = ["actor.session-reference-entities", "actor.market-state-entities", "actor.market-structure-entities"]
consumers = ["actor.evidence-health"]
parent_contracts = []
identity_fields = ["contract identity", "source identity", "instrument where applicable"]
join_key = []
expected_cardinality = "contract-specific and configuration-bounded"
event_clock = "source-defined or not applicable"
availability_clock = "local receipt or publication time where represented"
clock_unit = "UTC nanoseconds where the contract carries instants"
source_classes = ["verified_source"]
reconciliation_policy = "contract owner preserves duplicates, conflicts, revisions, and unknowns according to source contract"
retention = "transient unless explicitly persisted as an operational fact"
evidence = ["evidence.intelligence-contracts"]
limitations = []

[[contracts]]
id = "contract.evidence-health-snapshot"
label = "Evidence-health snapshot"
transport_kind = "nautilus_signal"
type_name = "markeitech.evidence.health.snapshot"
schema_version_kind = "explicit contract identity or framework-native type"
canonical_owner = "actor.evidence-health"
producers = ["actor.evidence-health"]
consumers = ["actor.session-reference-entities", "actor.market-state-entities", "actor.market-structure-entities"]
parent_contracts = []
identity_fields = ["contract identity", "source identity", "instrument where applicable"]
join_key = []
expected_cardinality = "contract-specific and configuration-bounded"
event_clock = "source-defined or not applicable"
availability_clock = "local receipt or publication time where represented"
clock_unit = "UTC nanoseconds where the contract carries instants"
source_classes = ["verified_source"]
reconciliation_policy = "contract owner preserves duplicates, conflicts, revisions, and unknowns according to source contract"
retention = "transient unless explicitly persisted as an operational fact"
evidence = ["evidence.intelligence-contracts"]
limitations = []

[[contracts]]
id = "contract.evidence-recency-profile"
label = "Evidence recency profile"
transport_kind = "nautilus_signal"
type_name = "markeitech.evidence.recency_profile"
schema_version_kind = "explicit contract identity or framework-native type"
canonical_owner = "actor.session-metrics"
producers = ["actor.session-metrics"]
consumers = ["actor.evidence-health"]
parent_contracts = []
identity_fields = ["contract identity", "source identity", "instrument where applicable"]
join_key = []
expected_cardinality = "contract-specific and configuration-bounded"
event_clock = "source-defined or not applicable"
availability_clock = "local receipt or publication time where represented"
clock_unit = "UTC nanoseconds where the contract carries instants"
source_classes = ["verified_source"]
reconciliation_policy = "contract owner preserves duplicates, conflicts, revisions, and unknowns according to source contract"
retention = "transient unless explicitly persisted as an operational fact"
evidence = ["evidence.intelligence-contracts"]
limitations = []

[[contracts]]
id = "contract.runtime-resource"
label = "Runtime resource sample"
transport_kind = "nautilus_signal"
type_name = "markeitech.runtime.resource"
schema_version_kind = "explicit contract identity or framework-native type"
canonical_owner = "actor.runtime-resources"
producers = ["actor.runtime-resources"]
consumers = ["actor.runtime-resource-health"]
parent_contracts = []
identity_fields = ["contract identity", "source identity", "instrument where applicable"]
join_key = []
expected_cardinality = "contract-specific and configuration-bounded"
event_clock = "source-defined or not applicable"
availability_clock = "local receipt or publication time where represented"
clock_unit = "UTC nanoseconds where the contract carries instants"
source_classes = ["verified_source"]
reconciliation_policy = "contract owner preserves duplicates, conflicts, revisions, and unknowns according to source contract"
retention = "transient unless explicitly persisted as an operational fact"
evidence = ["evidence.resource-contracts"]
limitations = []

[[contracts]]
id = "contract.runtime-health"
label = "Runtime resource health"
transport_kind = "nautilus_signal"
type_name = "markeitech.runtime.health"
schema_version_kind = "explicit contract identity or framework-native type"
canonical_owner = "actor.runtime-resource-health"
producers = ["actor.runtime-resource-health"]
consumers = ["actor.system-control", "actor.discord-health"]
parent_contracts = []
identity_fields = ["contract identity", "source identity", "instrument where applicable"]
join_key = []
expected_cardinality = "contract-specific and configuration-bounded"
event_clock = "source-defined or not applicable"
availability_clock = "local receipt or publication time where represented"
clock_unit = "UTC nanoseconds where the contract carries instants"
source_classes = ["verified_source"]
reconciliation_policy = "contract owner preserves duplicates, conflicts, revisions, and unknowns according to source contract"
retention = "transient unless explicitly persisted as an operational fact"
evidence = ["evidence.resource-contracts"]
limitations = []

[[contracts]]
id = "contract.completed-bar"
label = "Completed-bar input"
transport_kind = "nautilus_custom_data"
type_name = "markeitech.completed_bar.input"
schema_version_kind = "explicit contract identity or framework-native type"
canonical_owner = "actor.session-metrics"
producers = ["actor.session-metrics"]
consumers = ["actor.visual-debug", "actor.session-reference-entities", "actor.market-state-entities", "actor.market-structure-entities"]
parent_contracts = []
identity_fields = ["contract identity", "source identity", "instrument where applicable"]
join_key = []
expected_cardinality = "contract-specific and configuration-bounded"
event_clock = "interval-end normalized UTC nanoseconds"
availability_clock = "local receipt or publication time where represented"
clock_unit = "UTC nanoseconds where the contract carries instants"
source_classes = ["verified_source"]
reconciliation_policy = "contract owner preserves duplicates, conflicts, revisions, and unknowns according to source contract"
retention = "transient unless explicitly persisted as an operational fact"
evidence = ["evidence.completed-bar-contract"]
limitations = ["Historical and live lineage remain distinct and converge through explicit local admission policy."]

[[contracts]]
id = "contract.metric-value"
label = "Metric value"
transport_kind = "nautilus_custom_data"
type_name = "markeitech.metric.value"
schema_version_kind = "explicit contract identity or framework-native type"
canonical_owner = "actor.session-metrics"
producers = ["actor.session-metrics", "actor.quote-quality"]
consumers = ["actor.visual-debug", "actor.session-reference-entities", "actor.market-state-entities", "actor.market-structure-entities"]
parent_contracts = []
identity_fields = ["contract identity", "source identity", "instrument where applicable"]
join_key = []
expected_cardinality = "contract-specific and configuration-bounded"
event_clock = "source-defined or not applicable"
availability_clock = "local receipt or publication time where represented"
clock_unit = "UTC nanoseconds where the contract carries instants"
source_classes = ["verified_source"]
reconciliation_policy = "contract owner preserves duplicates, conflicts, revisions, and unknowns according to source contract"
retention = "transient unless explicitly persisted as an operational fact"
evidence = ["evidence.metric-contracts"]
limitations = []

[[contracts]]
id = "contract.entity-revision"
label = "Entity revision"
transport_kind = "nautilus_custom_data"
type_name = "markeitech.entity.revision"
schema_version_kind = "explicit contract identity or framework-native type"
canonical_owner = "actor.session-reference-entities"
producers = ["actor.session-reference-entities", "actor.market-state-entities", "actor.market-structure-entities"]
consumers = ["future.semantic-events"]
parent_contracts = []
identity_fields = ["contract identity", "source identity", "instrument where applicable"]
join_key = []
expected_cardinality = "contract-specific and configuration-bounded"
event_clock = "source-defined or not applicable"
availability_clock = "local receipt or publication time where represented"
clock_unit = "UTC nanoseconds where the contract carries instants"
source_classes = ["verified_source"]
reconciliation_policy = "contract owner preserves duplicates, conflicts, revisions, and unknowns according to source contract"
retention = "transient unless explicitly persisted as an operational fact"
evidence = ["evidence.entity-contracts"]
limitations = []

[[contracts]]
id = "contract.entity-snapshot-request"
label = "Entity snapshot request"
transport_kind = "nautilus_custom_data"
type_name = "markeitech.entity.snapshot.request"
schema_version_kind = "explicit contract identity or framework-native type"
canonical_owner = "future.semantic-events"
producers = ["future.semantic-events"]
consumers = ["actor.session-reference-entities", "actor.market-state-entities", "actor.market-structure-entities"]
parent_contracts = []
identity_fields = ["contract identity", "source identity", "instrument where applicable"]
join_key = []
expected_cardinality = "contract-specific and configuration-bounded"
event_clock = "source-defined or not applicable"
availability_clock = "local receipt or publication time where represented"
clock_unit = "UTC nanoseconds where the contract carries instants"
source_classes = ["verified_source"]
reconciliation_policy = "contract owner preserves duplicates, conflicts, revisions, and unknowns according to source contract"
retention = "transient unless explicitly persisted as an operational fact"
evidence = ["evidence.entity-contracts"]
limitations = []

[[contracts]]
id = "contract.entity-snapshot"
label = "Entity snapshot"
transport_kind = "nautilus_custom_data"
type_name = "markeitech.entity.snapshot"
schema_version_kind = "explicit contract identity or framework-native type"
canonical_owner = "actor.session-reference-entities"
producers = ["actor.session-reference-entities", "actor.market-state-entities", "actor.market-structure-entities"]
consumers = ["future.semantic-events"]
parent_contracts = []
identity_fields = ["contract identity", "source identity", "instrument where applicable"]
join_key = []
expected_cardinality = "contract-specific and configuration-bounded"
event_clock = "source-defined or not applicable"
availability_clock = "local receipt or publication time where represented"
clock_unit = "UTC nanoseconds where the contract carries instants"
source_classes = ["verified_source"]
reconciliation_policy = "contract owner preserves duplicates, conflicts, revisions, and unknowns according to source contract"
retention = "transient unless explicitly persisted as an operational fact"
evidence = ["evidence.entity-contracts"]
limitations = []

[[contracts]]
id = "contract.native-bar"
label = "Nautilus native bar"
transport_kind = "nautilus_native_data"
type_name = "nautilus.model.data.Bar"
schema_version_kind = "explicit contract identity or framework-native type"
canonical_owner = "component.data-engine"
producers = ["provider.interactive-brokers", "component.data-engine"]
consumers = ["actor.data-acquisition", "actor.session-metrics"]
parent_contracts = []
identity_fields = ["contract identity", "source identity", "instrument where applicable"]
join_key = []
expected_cardinality = "contract-specific and configuration-bounded"
event_clock = "provider start time translated by installed adapter"
availability_clock = "local receipt or publication time where represented"
clock_unit = "UTC nanoseconds where the contract carries instants"
source_classes = ["verified_source"]
reconciliation_policy = "contract owner preserves duplicates, conflicts, revisions, and unknowns according to source contract"
retention = "transient unless explicitly persisted as an operational fact"
evidence = ["evidence.node"]
limitations = ["Provider request method, exact contract qualification, entitlement, returned mode, and adapter mapping are unknown."]

[[contracts]]
id = "contract.native-subscription-command"
label = "Native subscription command"
transport_kind = "method_call"
type_name = "NautilusDataActor.subscribe_bars"
schema_version_kind = "explicit contract identity or framework-native type"
canonical_owner = "actor.data-acquisition"
producers = ["actor.data-acquisition"]
consumers = ["component.data-engine"]
parent_contracts = []
identity_fields = ["contract identity", "source identity", "instrument where applicable"]
join_key = []
expected_cardinality = "contract-specific and configuration-bounded"
event_clock = "source-defined or not applicable"
availability_clock = "local receipt or publication time where represented"
clock_unit = "UTC nanoseconds where the contract carries instants"
source_classes = ["verified_source"]
reconciliation_policy = "contract owner preserves duplicates, conflicts, revisions, and unknowns according to source contract"
retention = "transient unless explicitly persisted as an operational fact"
evidence = ["evidence.node"]
limitations = ["A returned call is local command issuance, not provider acknowledgement."]

[[contracts]]
id = "contract.native-historical-request"
label = "Native historical request"
transport_kind = "method_call"
type_name = "NautilusDataActor.request_bars"
schema_version_kind = "explicit contract identity or framework-native type"
canonical_owner = "actor.data-acquisition"
producers = ["actor.data-acquisition"]
consumers = ["component.data-engine"]
parent_contracts = []
identity_fields = ["contract identity", "source identity", "instrument where applicable"]
join_key = []
expected_cardinality = "contract-specific and configuration-bounded"
event_clock = "source-defined or not applicable"
availability_clock = "local receipt or publication time where represented"
clock_unit = "UTC nanoseconds where the contract carries instants"
source_classes = ["verified_source"]
reconciliation_policy = "contract owner preserves duplicates, conflicts, revisions, and unknowns according to source contract"
retention = "transient unless explicitly persisted as an operational fact"
evidence = ["evidence.historical-contracts"]
limitations = ["Exact IB method mapping and cancellation are unknown."]

[[contracts]]
id = "contract.operational-event"
label = "Operational audit event"
transport_kind = "thread_queue"
type_name = "markeitech.operational.event"
schema_version_kind = "explicit contract identity or framework-native type"
canonical_owner = "actor.operational-persistence"
producers = ["actor.operational-persistence"]
consumers = ["queue.persistence", "worker.persistence", "store.postgres"]
parent_contracts = []
identity_fields = ["contract identity", "source identity", "instrument where applicable"]
join_key = []
expected_cardinality = "contract-specific and configuration-bounded"
event_clock = "application availability and lifecycle time"
availability_clock = "local receipt or publication time where represented"
clock_unit = "UTC nanoseconds where the contract carries instants"
source_classes = ["verified_source"]
reconciliation_policy = "contract owner preserves duplicates, conflicts, revisions, and unknowns according to source contract"
retention = "durable approved operational facts only; not raw provider observations"
evidence = ["evidence.persistence"]
limitations = []

[[contracts]]
id = "contract.persistence-result"
label = "Persistence worker result"
transport_kind = "thread_queue"
type_name = "markeitech.persistence.result"
schema_version_kind = "explicit contract identity or framework-native type"
canonical_owner = "actor.operational-persistence"
producers = ["worker.persistence"]
consumers = ["actor.operational-persistence"]
parent_contracts = []
identity_fields = ["contract identity", "source identity", "instrument where applicable"]
join_key = []
expected_cardinality = "contract-specific and configuration-bounded"
event_clock = "source-defined or not applicable"
availability_clock = "local receipt or publication time where represented"
clock_unit = "UTC nanoseconds where the contract carries instants"
source_classes = ["verified_source"]
reconciliation_policy = "contract owner preserves duplicates, conflicts, revisions, and unknowns according to source contract"
retention = "transient unless explicitly persisted as an operational fact"
evidence = ["evidence.persistence"]
limitations = []

[[contracts]]
id = "contract.discord-notification"
label = "Discord health notification"
transport_kind = "thread_queue"
type_name = "markeitech.discord.health"
schema_version_kind = "explicit contract identity or framework-native type"
canonical_owner = "actor.discord-health"
producers = ["actor.discord-health"]
consumers = ["queue.discord", "worker.discord", "projection.discord"]
parent_contracts = []
identity_fields = ["contract identity", "source identity", "instrument where applicable"]
join_key = []
expected_cardinality = "contract-specific and configuration-bounded"
event_clock = "source-defined or not applicable"
availability_clock = "local receipt or publication time where represented"
clock_unit = "UTC nanoseconds where the contract carries instants"
source_classes = ["verified_source"]
reconciliation_policy = "contract owner preserves duplicates, conflicts, revisions, and unknowns according to source contract"
retention = "transient unless explicitly persisted as an operational fact"
evidence = ["evidence.discord"]
limitations = []

[[contracts]]
id = "contract.visual-artifact"
label = "Visual debug artifact"
transport_kind = "filesystem"
type_name = "markeitech.visual-debug.capture"
schema_version_kind = "explicit contract identity or framework-native type"
canonical_owner = "actor.visual-debug"
producers = ["actor.visual-debug"]
consumers = ["projection.visual-files", "operator.markeitect"]
parent_contracts = []
identity_fields = ["contract identity", "source identity", "instrument where applicable"]
join_key = []
expected_cardinality = "contract-specific and configuration-bounded"
event_clock = "not applicable"
availability_clock = "local receipt or publication time where represented"
clock_unit = "UTC nanoseconds where the contract carries instants"
source_classes = ["verified_source"]
reconciliation_policy = "contract owner preserves duplicates, conflicts, revisions, and unknowns according to source contract"
retention = "operator-controlled diagnostic retention"
evidence = ["evidence.visual-debug"]
limitations = ["Projection only; no canonical market or architecture authority."]

[[contracts]]
id = "contract.future-semantic-event"
label = "Future semantic event"
transport_kind = "nautilus_custom_data"
type_name = "future.markeitech.semantic-event"
schema_version_kind = "explicit contract identity or framework-native type"
canonical_owner = "future.semantic-events"
producers = ["future.semantic-events"]
consumers = ["future.opportunities", "future.sir-loke"]
parent_contracts = []
identity_fields = ["contract identity", "source identity", "instrument where applicable"]
join_key = []
expected_cardinality = "contract-specific and configuration-bounded"
event_clock = "source-defined or not applicable"
availability_clock = "local receipt or publication time where represented"
clock_unit = "UTC nanoseconds where the contract carries instants"
source_classes = ["verified_source"]
reconciliation_policy = "contract owner preserves duplicates, conflicts, revisions, and unknowns according to source contract"
retention = "transient unless explicitly persisted as an operational fact"
evidence = ["evidence.roadmap"]
limitations = ["Future, not implemented."]

[[contracts]]
id = "contract.future-opportunity"
label = "Future plural opportunity"
transport_kind = "nautilus_custom_data"
type_name = "future.markeitech.opportunity"
schema_version_kind = "explicit contract identity or framework-native type"
canonical_owner = "future.opportunities"
producers = ["future.opportunities"]
consumers = ["future.sir-loke"]
parent_contracts = []
identity_fields = ["contract identity", "source identity", "instrument where applicable"]
join_key = []
expected_cardinality = "contract-specific and configuration-bounded"
event_clock = "source-defined or not applicable"
availability_clock = "local receipt or publication time where represented"
clock_unit = "UTC nanoseconds where the contract carries instants"
source_classes = ["verified_source"]
reconciliation_policy = "contract owner preserves duplicates, conflicts, revisions, and unknowns according to source contract"
retention = "transient unless explicitly persisted as an operational fact"
evidence = ["evidence.roadmap"]
limitations = ["Future, not implemented."]

[[contracts]]
id = "contract.future-advisory-intent"
label = "Future governed advisory intent"
transport_kind = "not_applicable"
type_name = "future.markeitech.advisory-intent"
schema_version_kind = "explicit contract identity or framework-native type"
canonical_owner = "future.sir-loke"
producers = ["future.sir-loke"]
consumers = ["operator.markeitect", "future.controlled-execution"]
parent_contracts = []
identity_fields = ["contract identity", "source identity", "instrument where applicable"]
join_key = []
expected_cardinality = "contract-specific and configuration-bounded"
event_clock = "source-defined or not applicable"
availability_clock = "local receipt or publication time where represented"
clock_unit = "UTC nanoseconds where the contract carries instants"
source_classes = ["verified_source"]
reconciliation_policy = "contract owner preserves duplicates, conflicts, revisions, and unknowns according to source contract"
retention = "transient unless explicitly persisted as an operational fact"
evidence = ["evidence.roadmap"]
limitations = ["Future, does not grant execution authority."]

[[contracts]]
id = "contract.future-approval"
label = "Future explicit approval"
transport_kind = "not_applicable"
type_name = "future.markeitech.execution-approval"
schema_version_kind = "explicit contract identity or framework-native type"
canonical_owner = "operator.markeitect"
producers = ["operator.markeitect"]
consumers = ["future.controlled-execution"]
parent_contracts = []
identity_fields = ["contract identity", "source identity", "instrument where applicable"]
join_key = []
expected_cardinality = "contract-specific and configuration-bounded"
event_clock = "source-defined or not applicable"
availability_clock = "local receipt or publication time where represented"
clock_unit = "UTC nanoseconds where the contract carries instants"
source_classes = ["verified_source"]
reconciliation_policy = "contract owner preserves duplicates, conflicts, revisions, and unknowns according to source contract"
retention = "transient unless explicitly persisted as an operational fact"
evidence = ["evidence.roadmap"]
limitations = ["Future, separately governed and not implemented."]

[[delivery_claims]]
id = "delivery.signal-local"
scope = "In-process Nautilus signal publication"
transport = "nautilus_signal"
guarantee = "unknown"
replay = "no replay guarantee"
acknowledgement_milestone = "publish call or consumer-specific response where implemented"
ordering_scope = "publisher order only where framework/source establishes it"
cross_producer_ordering = "unknown"
duplicate_possible = true
evidence = ["evidence.system-contracts"]
limitations = ["Late subscribers may miss prior publications."]

[[delivery_claims]]
id = "delivery.custom-data-local"
scope = "In-process Nautilus custom-data publication"
transport = "nautilus_custom_data"
guarantee = "unknown"
replay = "no startup replay guarantee"
acknowledgement_milestone = "publication admission only"
ordering_scope = "publisher-local only"
cross_producer_ordering = "unknown"
duplicate_possible = true
evidence = ["evidence.metric-contracts"]
limitations = []

[[delivery_claims]]
id = "delivery.native-provider-unknown"
scope = "Provider through installed native data path"
transport = "nautilus_native_data"
guarantee = "unknown"
replay = "provider and adapter dependent"
acknowledgement_milestone = "first matching observation is delivery evidence, not continuity"
ordering_scope = "unknown"
cross_producer_ordering = "unknown"
duplicate_possible = true
evidence = ["evidence.node"]
limitations = ["No provider acknowledgement, entitlement, continuity, or exactly-once claim."]

[[delivery_claims]]
id = "delivery.method-command"
scope = "Local native method command"
transport = "method_call"
guarantee = "at_most_once_attempt"
replay = "not replayed by this edge"
acknowledgement_milestone = "method return without immediate exception"
ordering_scope = "caller order only"
cross_producer_ordering = "not applicable"
duplicate_possible = false
evidence = ["evidence.node"]
limitations = ["Does not prove provider acceptance."]

[[delivery_claims]]
id = "delivery.queue-bounded"
scope = "Bounded local queue admission"
transport = "thread_queue"
guarantee = "at_least_once_attempt"
replay = "bounded retry is component-specific"
acknowledgement_milestone = "queue admission is not durable storage or external delivery"
ordering_scope = "single producer/queue sequence only where implemented"
cross_producer_ordering = "unknown"
duplicate_possible = true
evidence = ["evidence.persistence"]
limitations = ["Worker failure and ambiguous commit remain explicit."]

[[delivery_claims]]
id = "delivery.filesystem-projection"
scope = "Filesystem projection attempt"
transport = "filesystem"
guarantee = "at_most_once_attempt"
replay = "operator may regenerate runtime diagnostic separately"
acknowledgement_milestone = "atomic artifact publication where implemented"
ordering_scope = "single capture writer"
cross_producer_ordering = "not applicable"
duplicate_possible = false
evidence = ["evidence.visual-debug"]
limitations = []

[[delivery_claims]]
id = "delivery.future-none"
scope = "Future relationship only"
transport = "not_applicable"
guarantee = "not_applicable"
replay = "not implemented"
acknowledgement_milestone = "not applicable"
ordering_scope = "not applicable"
cross_producer_ordering = "not applicable"
duplicate_possible = false
evidence = ["evidence.roadmap"]
limitations = ["No current transport or delivery semantics."]

[[edges]]
id = "edge.ib-native-bars"
source = "provider.interactive-brokers"
target = "component.data-engine"
category = "native_observation"
contract = "contract.native-bar"
transport = "nautilus_native_data"
correlation = "contract identity plus explicit source/instrument/request identity where verified"
sync_semantics = "verified_async"
delivery_claim = "delivery.native-provider-unknown"
authority_direction = "source publishes or commands; canonical ownership remains declared on the contract"
required = true
style = "style.edge.data"
evidence = ["evidence.node"]
limitations = ["External capability edge; exact IB method/callback mapping is unknown."]

[[edges]]
id = "edge.acquisition-subscribe"
source = "actor.data-acquisition"
target = "component.data-engine"
category = "subscription_command"
contract = "contract.native-subscription-command"
transport = "method_call"
correlation = "contract identity plus explicit source/instrument/request identity where verified"
sync_semantics = "verified_sync"
delivery_claim = "delivery.method-command"
enablement_condition = "watchlist.capabilities.watchlist_last"
authority_direction = "source publishes or commands; canonical ownership remains declared on the contract"
required = true
style = "style.edge.data"
evidence = ["evidence.node"]
limitations = ["Native command issued is not provider acknowledgement."]

[[edges]]
id = "edge.data-engine-live-callback"
source = "component.data-engine"
target = "actor.data-acquisition"
category = "callback"
contract = "contract.native-bar"
transport = "nautilus_callback"
correlation = "contract identity plus explicit source/instrument/request identity where verified"
sync_semantics = "verified_async"
delivery_claim = "delivery.native-provider-unknown"
authority_direction = "source publishes or commands; canonical ownership remains declared on the contract"
required = true
style = "style.edge.data"
evidence = ["evidence.composition"]
limitations = []

[[edges]]
id = "edge.data-engine-metric-bars"
source = "component.data-engine"
target = "actor.session-metrics"
category = "native_observation"
contract = "contract.native-bar"
transport = "nautilus_native_data"
correlation = "contract identity plus explicit source/instrument/request identity where verified"
sync_semantics = "verified_async"
delivery_claim = "delivery.native-provider-unknown"
authority_direction = "source publishes or commands; canonical ownership remains declared on the contract"
required = true
style = "style.edge.data"
evidence = ["evidence.composition"]
limitations = []

[[edges]]
id = "edge.watchlist-demand"
source = "actor.watchlist"
target = "actor.data-acquisition"
category = "publication"
contract = "contract.watchlist-demand"
transport = "nautilus_signal"
correlation = "contract identity plus explicit source/instrument/request identity where verified"
sync_semantics = "verified_async"
delivery_claim = "delivery.signal-local"
authority_direction = "source publishes or commands; canonical ownership remains declared on the contract"
required = true
style = "style.edge.data"
evidence = ["evidence.composition"]
limitations = []

[[edges]]
id = "edge.watchlist-status-request"
source = "actor.watchlist"
target = "actor.data-acquisition"
category = "query"
contract = "contract.acquisition-status-request"
transport = "nautilus_signal"
correlation = "contract identity plus explicit source/instrument/request identity where verified"
sync_semantics = "verified_async"
delivery_claim = "delivery.signal-local"
authority_direction = "source publishes or commands; canonical ownership remains declared on the contract"
required = true
style = "style.edge.data"
evidence = ["evidence.composition"]
limitations = []

[[edges]]
id = "edge.acquisition-status-watchlist"
source = "actor.data-acquisition"
target = "actor.watchlist"
category = "response"
contract = "contract.acquisition-status"
transport = "nautilus_signal"
correlation = "contract identity plus explicit source/instrument/request identity where verified"
sync_semantics = "verified_async"
delivery_claim = "delivery.signal-local"
authority_direction = "source publishes or commands; canonical ownership remains declared on the contract"
required = true
style = "style.edge.data"
evidence = ["evidence.composition"]
limitations = []

[[edges]]
id = "edge.acquisition-status-system"
source = "actor.data-acquisition"
target = "actor.system-control"
category = "publication"
contract = "contract.acquisition-status"
transport = "nautilus_signal"
correlation = "contract identity plus explicit source/instrument/request identity where verified"
sync_semantics = "verified_async"
delivery_claim = "delivery.signal-local"
authority_direction = "source publishes or commands; canonical ownership remains declared on the contract"
required = true
style = "style.edge.data"
evidence = ["evidence.composition"]
limitations = []

[[edges]]
id = "edge.acquisition-stream-health"
source = "actor.data-acquisition"
target = "actor.evidence-health"
category = "publication"
contract = "contract.acquisition-stream"
transport = "nautilus_signal"
correlation = "contract identity plus explicit source/instrument/request identity where verified"
sync_semantics = "verified_async"
delivery_claim = "delivery.signal-local"
authority_direction = "source publishes or commands; canonical ownership remains declared on the contract"
required = true
style = "style.edge.data"
evidence = ["evidence.composition"]
limitations = []

[[edges]]
id = "edge.calendar-request-health"
source = "actor.evidence-health"
target = "actor.session-state"
category = "query"
contract = "contract.calendar-state-snapshot-request"
transport = "nautilus_custom_data"
correlation = "cycle_id plus request_id plus requester plus attempt plus current source epoch"
sync_semantics = "verified_async"
delivery_claim = "delivery.custom-data-local"
authority_direction = "Evidence Health requests current state; Session State retains calendar authority."
required = true
style = "style.edge.data"
evidence = ["evidence.calendar-contracts"]
limitations = ["Bounded local retry has no replay guarantee."]

[[edges]]
id = "edge.calendar-response-health"
source = "actor.session-state"
target = "actor.evidence-health"
category = "response"
contract = "contract.calendar-state-snapshot-response"
transport = "nautilus_custom_data"
correlation = "cycle_id plus request_id plus requester plus attempt plus source plus source_epoch"
sync_semantics = "verified_async"
delivery_claim = "delivery.custom-data-local"
authority_direction = "Session State supplies reconciliable point-in-time state; Evidence Health does not redefine it."
required = true
style = "style.edge.data"
evidence = ["evidence.calendar-contracts"]
limitations = []

[[edges]]
id = "edge.calendar-request-metrics"
source = "actor.session-metrics"
target = "actor.session-state"
category = "query"
contract = "contract.calendar-projection-request"
transport = "nautilus_custom_data"
correlation = "request_id plus requester plus current source epoch"
sync_semantics = "verified_async"
delivery_claim = "delivery.custom-data-local"
authority_direction = "Session Metrics requests context; Session State retains calendar authority."
required = true
style = "style.edge.data"
evidence = ["evidence.calendar-contracts"]
limitations = ["Bounded local retry has no replay guarantee."]

[[edges]]
id = "edge.calendar-response-metrics"
source = "actor.session-state"
target = "actor.session-metrics"
category = "response"
contract = "contract.calendar-projection-response"
transport = "nautilus_custom_data"
correlation = "request_id plus requester plus source plus source_epoch"
sync_semantics = "verified_async"
delivery_claim = "delivery.custom-data-local"
authority_direction = "Session State supplies definition-identified projections; Session Metrics consumes immutable views."
required = true
style = "style.edge.data"
evidence = ["evidence.calendar-contracts"]
limitations = []

[[edges]]
id = "edge.calendar-request-planner"
source = "actor.historical-planner"
target = "actor.session-state"
category = "query"
contract = "contract.calendar-projection-request"
transport = "nautilus_custom_data"
correlation = "request_id plus requester plus current source epoch"
sync_semantics = "verified_async"
delivery_claim = "delivery.custom-data-local"
authority_direction = "Historical Planner requests context; Session State retains calendar authority."
required = true
style = "style.edge.data"
evidence = ["evidence.calendar-contracts", "evidence.historical-planner"]
limitations = ["Bounded local retry has no replay guarantee."]

[[edges]]
id = "edge.calendar-response-planner"
source = "actor.session-state"
target = "actor.historical-planner"
category = "response"
contract = "contract.calendar-projection-response"
transport = "nautilus_custom_data"
correlation = "request_id plus requester plus source plus source_epoch"
sync_semantics = "verified_async"
delivery_claim = "delivery.custom-data-local"
authority_direction = "Session State supplies definition-identified projections; Historical Planner resolves exact request bounds."
required = true
style = "style.edge.data"
evidence = ["evidence.calendar-contracts", "evidence.historical-planner"]
limitations = []

[[edges]]
id = "edge.calendar-state-request-planner"
source = "actor.historical-planner"
target = "actor.session-state"
category = "query"
contract = "contract.calendar-state-snapshot-request"
transport = "nautilus_custom_data"
correlation = "cycle_id plus request_id plus requester plus attempt plus current source epoch"
sync_semantics = "verified_async"
delivery_claim = "delivery.custom-data-local"
authority_direction = "Historical Planner requests current context; Session State retains calendar authority."
required = true
style = "style.edge.data"
evidence = ["evidence.calendar-contracts", "evidence.historical-planner"]
limitations = ["Transient local publication requires bounded consumer retry."]

[[edges]]
id = "edge.calendar-state-response-planner"
source = "actor.session-state"
target = "actor.historical-planner"
category = "response"
contract = "contract.calendar-state-snapshot-response"
transport = "nautilus_custom_data"
correlation = "cycle_id plus request_id plus requester plus attempt plus source plus source_epoch"
sync_semantics = "verified_async"
delivery_claim = "delivery.custom-data-local"
authority_direction = "Session State supplies current state; Historical Planner releases deferred demand only after gap-free reconciliation."
required = true
style = "style.edge.data"
evidence = ["evidence.calendar-contracts", "evidence.historical-planner"]
limitations = []

[[edges]]
id = "edge.calendar-transition-health"
source = "actor.session-state"
target = "actor.evidence-health"
category = "publication"
contract = "contract.calendar-transition"
transport = "nautilus_custom_data"
correlation = "calendar_id plus definition_digest plus source_epoch plus revision"
sync_semantics = "verified_async"
delivery_claim = "delivery.custom-data-local"
authority_direction = "Session State publishes canonical temporal context."
required = true
style = "style.edge.data"
evidence = ["evidence.calendar-contracts"]
limitations = []

[[edges]]
id = "edge.calendar-transition-planner"
source = "actor.session-state"
target = "actor.historical-planner"
category = "publication"
contract = "contract.calendar-transition"
transport = "nautilus_custom_data"
correlation = "calendar_id plus definition_digest plus source_epoch plus revision"
sync_semantics = "verified_async"
delivery_claim = "delivery.custom-data-local"
authority_direction = "Session State publishes canonical temporal context."
required = true
style = "style.edge.data"
evidence = ["evidence.calendar-contracts", "evidence.historical-planner"]
limitations = []

[[edges]]
id = "edge.calendar-transition-persistence"
source = "actor.session-state"
target = "actor.operational-persistence"
category = "persistence"
contract = "contract.calendar-transition"
transport = "nautilus_custom_data"
correlation = "event_id plus source_epoch plus revision"
sync_semantics = "verified_async"
delivery_claim = "delivery.custom-data-local"
authority_direction = "Operational persistence records the transition fact without becoming calendar authority."
required = true
style = "style.edge.persistence"
evidence = ["evidence.calendar-contracts", "evidence.persistence"]
limitations = ["Projection payloads and raw schedules are not persisted."]

[[edges]]
id = "edge.calendar-state-request-probe"
source = "actor.current-state-historical-probe"
target = "actor.session-state"
category = "query"
contract = "contract.calendar-state-snapshot-request"
transport = "nautilus_custom_data"
correlation = "cycle_id plus request_id plus exact requester plus attempt plus current source epoch"
sync_semantics = "verified_async"
delivery_claim = "delivery.custom-data-local"
enablement_condition = "historical.probe.enabled"
authority_direction = "The acceptance probe requests current state; Session State retains calendar authority."
required = true
style = "style.edge.data"
evidence = ["evidence.calendar-contracts", "evidence.composition"]
limitations = ["Composition additionally requires historical.probe.mode=current_state_gated."]

[[edges]]
id = "edge.calendar-state-response-probe"
source = "actor.session-state"
target = "actor.current-state-historical-probe"
category = "response"
contract = "contract.calendar-state-snapshot-response"
transport = "nautilus_custom_data"
correlation = "cycle_id plus request_id plus exact requester plus attempt plus source plus source_epoch"
sync_semantics = "verified_async"
delivery_claim = "delivery.custom-data-local"
enablement_condition = "historical.probe.enabled"
authority_direction = "Session State supplies current state; the probe gates one demand on gap-free reconciliation."
required = true
style = "style.edge.data"
evidence = ["evidence.calendar-contracts", "evidence.composition"]
limitations = ["Composition additionally requires historical.probe.mode=current_state_gated."]

[[edges]]
id = "edge.calendar-transition-probe"
source = "actor.session-state"
target = "actor.current-state-historical-probe"
category = "publication"
contract = "contract.calendar-transition"
transport = "nautilus_custom_data"
correlation = "calendar_id plus definition_digest plus source_epoch plus revision"
sync_semantics = "verified_async"
delivery_claim = "delivery.custom-data-local"
enablement_condition = "historical.probe.enabled"
authority_direction = "Session State publishes canonical temporal context; the probe only reconciles it."
required = true
style = "style.edge.data"
evidence = ["evidence.calendar-contracts", "evidence.composition"]
limitations = ["Composition additionally requires historical.probe.mode=current_state_gated."]

[[edges]]
id = "edge.analytical-demand"
source = "actor.session-metrics"
target = "actor.data-acquisition"
category = "publication"
contract = "contract.analytical-demand"
transport = "nautilus_signal"
correlation = "contract identity plus explicit source/instrument/request identity where verified"
sync_semantics = "verified_async"
delivery_claim = "delivery.signal-local"
authority_direction = "source publishes or commands; canonical ownership remains declared on the contract"
required = true
style = "style.edge.data"
evidence = ["evidence.composition"]
limitations = []

[[edges]]
id = "edge.historical-demand-planner"
source = "actor.session-metrics"
target = "actor.historical-planner"
category = "query"
contract = "contract.historical-demand"
transport = "nautilus_signal"
correlation = "contract identity plus explicit source/instrument/request identity where verified"
sync_semantics = "verified_async"
delivery_claim = "delivery.signal-local"
authority_direction = "source publishes or commands; canonical ownership remains declared on the contract"
required = true
style = "style.edge.data"
evidence = ["evidence.historical-planner"]
limitations = []

[[edges]]
id = "edge.historical-plan-acquisition"
source = "actor.historical-planner"
target = "actor.data-acquisition"
category = "publication"
contract = "contract.historical-request-plan"
transport = "nautilus_custom_data"
correlation = "demand_id plus request.request_id plus calendar_definition_digest"
sync_semantics = "verified_async"
delivery_claim = "delivery.custom-data-local"
authority_direction = "Historical Planner owns calendar-bound resolution; Data Acquisition owns provider execution."
required = true
style = "style.edge.data"
evidence = ["evidence.historical-planner", "evidence.historical-contracts"]
limitations = ["Plan delivery does not prove provider acceptance or complete observations."]

[[edges]]
id = "edge.current-state-probe-historical-demand"
source = "actor.current-state-historical-probe"
target = "actor.historical-planner"
category = "query"
contract = "contract.historical-demand"
transport = "nautilus_signal"
correlation = "stable demand_id plus exact consumer_id, instrument, selector, and as_of_ns"
sync_semantics = "verified_async"
delivery_claim = "delivery.signal-local"
enablement_condition = "historical.probe.enabled"
authority_direction = "The probe declares one symbolic need; Historical Planner owns exact calendar-bound resolution."
required = true
style = "style.edge.data"
evidence = ["evidence.composition", "evidence.historical-planner"]
limitations = ["Composition additionally requires historical.probe.mode=current_state_gated."]

[[edges]]
id = "edge.historical-plan-current-state-probe"
source = "actor.historical-planner"
target = "actor.current-state-historical-probe"
category = "response"
contract = "contract.historical-request-plan"
transport = "nautilus_custom_data"
correlation = "demand_id plus request.request_id plus calendar_definition_digest"
sync_semantics = "verified_async"
delivery_claim = "delivery.custom-data-local"
enablement_condition = "historical.probe.enabled"
authority_direction = "The probe observes the exact plan without changing or executing it."
required = true
style = "style.edge.data"
evidence = ["evidence.composition", "evidence.historical-planner"]
limitations = ["Plan observation does not prove provider acceptance or complete observations."]

[[edges]]
id = "edge.native-historical-request"
source = "actor.data-acquisition"
target = "component.data-engine"
category = "query"
contract = "contract.native-historical-request"
transport = "method_call"
correlation = "contract identity plus explicit source/instrument/request identity where verified"
sync_semantics = "verified_sync"
delivery_claim = "delivery.method-command"
enablement_condition = "historical.enabled"
authority_direction = "source publishes or commands; canonical ownership remains declared on the contract"
required = true
style = "style.edge.data"
evidence = ["evidence.historical-contracts"]
limitations = ["Local request has one active lane and no verified provider cancellation."]

[[edges]]
id = "edge.historical-provider-response"
source = "provider.interactive-brokers"
target = "component.data-engine"
category = "response"
contract = "contract.native-bar"
transport = "nautilus_native_data"
correlation = "contract identity plus explicit source/instrument/request identity where verified"
sync_semantics = "verified_async"
delivery_claim = "delivery.native-provider-unknown"
enablement_condition = "historical.enabled"
authority_direction = "source publishes or commands; canonical ownership remains declared on the contract"
required = true
style = "style.edge.data"
evidence = ["evidence.node"]
limitations = []

[[edges]]
id = "edge.historical-batch"
source = "actor.data-acquisition"
target = "actor.session-metrics"
category = "response"
contract = "contract.historical-batch"
transport = "nautilus_custom_data"
correlation = "contract identity plus explicit source/instrument/request identity where verified"
sync_semantics = "verified_async"
delivery_claim = "delivery.custom-data-local"
enablement_condition = "metrics.session_measurements.enabled"
authority_direction = "source publishes or commands; canonical ownership remains declared on the contract"
required = true
style = "style.edge.data"
evidence = ["evidence.historical-contracts"]
limitations = []

[[edges]]
id = "edge.historical-readiness"
source = "actor.data-acquisition"
target = "actor.session-metrics"
category = "readiness"
contract = "contract.historical-readiness"
transport = "nautilus_signal"
correlation = "contract identity plus explicit source/instrument/request identity where verified"
sync_semantics = "verified_async"
delivery_claim = "delivery.signal-local"
enablement_condition = "metrics.session_measurements.enabled"
authority_direction = "source publishes or commands; canonical ownership remains declared on the contract"
required = true
style = "style.edge.data"
evidence = ["evidence.historical-contracts"]
limitations = []

[[edges]]
id = "edge.historical-batch-current-state-probe"
source = "actor.data-acquisition"
target = "actor.current-state-historical-probe"
category = "response"
contract = "contract.historical-batch"
transport = "nautilus_custom_data"
correlation = "request dependency consumer_id plus request_id"
sync_semantics = "verified_async"
delivery_claim = "delivery.custom-data-local"
enablement_condition = "historical.probe.enabled"
authority_direction = "Data Acquisition publishes the transient provider result; the probe only counts correlated evidence."
required = true
style = "style.edge.data"
evidence = ["evidence.historical-contracts", "evidence.composition"]
limitations = ["Composition additionally requires historical.probe.mode=current_state_gated.", "A batch does not alone prove completeness."]

[[edges]]
id = "edge.historical-readiness-current-state-probe"
source = "actor.data-acquisition"
target = "actor.current-state-historical-probe"
category = "readiness"
contract = "contract.historical-readiness"
transport = "nautilus_signal"
correlation = "consumer_id plus request_id"
sync_semantics = "verified_async"
delivery_claim = "delivery.signal-local"
enablement_condition = "historical.probe.enabled"
authority_direction = "Data Acquisition reports readiness; the probe only records its correlated acceptance outcome."
required = true
style = "style.edge.data"
evidence = ["evidence.historical-contracts", "evidence.composition"]
limitations = ["Composition additionally requires historical.probe.mode=current_state_gated."]

[[edges]]
id = "edge.completed-bars-visual"
source = "actor.session-metrics"
target = "actor.visual-debug"
category = "projection"
contract = "contract.completed-bar"
transport = "nautilus_custom_data"
correlation = "contract identity plus explicit source/instrument/request identity where verified"
sync_semantics = "verified_async"
delivery_claim = "delivery.custom-data-local"
enablement_condition = "visual_debug_capture.enabled"
authority_direction = "source publishes or commands; canonical ownership remains declared on the contract"
required = true
style = "style.edge.projection"
evidence = ["evidence.visual-debug"]
limitations = []

[[edges]]
id = "edge.metrics-visual"
source = "actor.session-metrics"
target = "actor.visual-debug"
category = "projection"
contract = "contract.metric-value"
transport = "nautilus_custom_data"
correlation = "contract identity plus explicit source/instrument/request identity where verified"
sync_semantics = "verified_async"
delivery_claim = "delivery.custom-data-local"
enablement_condition = "visual_debug_capture.enabled"
authority_direction = "source publishes or commands; canonical ownership remains declared on the contract"
required = true
style = "style.edge.projection"
evidence = ["evidence.visual-debug"]
limitations = []

[[edges]]
id = "edge.visual-files"
source = "actor.visual-debug"
target = "projection.visual-files"
category = "projection"
contract = "contract.visual-artifact"
transport = "filesystem"
correlation = "contract identity plus explicit source/instrument/request identity where verified"
sync_semantics = "verified_async"
delivery_claim = "delivery.filesystem-projection"
enablement_condition = "visual_debug_capture.enabled"
authority_direction = "source publishes or commands; canonical ownership remains declared on the contract"
required = true
style = "style.edge.projection"
evidence = ["evidence.visual-debug"]
limitations = []

[[edges]]
id = "edge.visual-review"
source = "projection.visual-files"
target = "operator.markeitect"
category = "projection"
contract = "contract.visual-artifact"
transport = "filesystem"
correlation = "contract identity plus explicit source/instrument/request identity where verified"
sync_semantics = "verified_async"
delivery_claim = "delivery.filesystem-projection"
authority_direction = "source publishes or commands; canonical ownership remains declared on the contract"
required = false
style = "style.edge.projection"
evidence = ["evidence.visual-debug"]
limitations = []

[[edges]]
id = "edge.completed-bars-session-entities"
source = "actor.session-metrics"
target = "actor.session-reference-entities"
category = "publication"
contract = "contract.completed-bar"
transport = "nautilus_custom_data"
correlation = "contract identity plus explicit source/instrument/request identity where verified"
sync_semantics = "verified_async"
delivery_claim = "delivery.custom-data-local"
enablement_condition = "metrics.entity_analysis.enabled"
authority_direction = "source publishes or commands; canonical ownership remains declared on the contract"
required = false
style = "style.edge.data"
evidence = ["evidence.entity-contracts"]
limitations = []

[[edges]]
id = "edge.metrics-market-state"
source = "actor.session-metrics"
target = "actor.market-state-entities"
category = "publication"
contract = "contract.metric-value"
transport = "nautilus_custom_data"
correlation = "contract identity plus explicit source/instrument/request identity where verified"
sync_semantics = "verified_async"
delivery_claim = "delivery.custom-data-local"
enablement_condition = "metrics.entity_analysis.enabled"
authority_direction = "source publishes or commands; canonical ownership remains declared on the contract"
required = false
style = "style.edge.data"
evidence = ["evidence.entity-contracts"]
limitations = []

[[edges]]
id = "edge.metrics-market-structure"
source = "actor.session-metrics"
target = "actor.market-structure-entities"
category = "publication"
contract = "contract.metric-value"
transport = "nautilus_custom_data"
correlation = "contract identity plus explicit source/instrument/request identity where verified"
sync_semantics = "verified_async"
delivery_claim = "delivery.custom-data-local"
enablement_condition = "metrics.entity_analysis.enabled"
authority_direction = "source publishes or commands; canonical ownership remains declared on the contract"
required = false
style = "style.edge.data"
evidence = ["evidence.entity-contracts"]
limitations = []

[[edges]]
id = "edge.recency-profile"
source = "actor.session-metrics"
target = "actor.evidence-health"
category = "publication"
contract = "contract.evidence-recency-profile"
transport = "nautilus_signal"
correlation = "contract identity plus explicit source/instrument/request identity where verified"
sync_semantics = "verified_async"
delivery_claim = "delivery.signal-local"
enablement_condition = "metrics.session_measurements.enabled"
authority_direction = "source publishes or commands; canonical ownership remains declared on the contract"
required = true
style = "style.edge.data"
evidence = ["evidence.intelligence-contracts"]
limitations = []

[[edges]]
id = "edge.health-snapshot-request"
source = "actor.market-state-entities"
target = "actor.evidence-health"
category = "query"
contract = "contract.evidence-health-snapshot-request"
transport = "nautilus_signal"
correlation = "contract identity plus explicit source/instrument/request identity where verified"
sync_semantics = "verified_async"
delivery_claim = "delivery.signal-local"
enablement_condition = "metrics.entity_analysis.enabled"
authority_direction = "source publishes or commands; canonical ownership remains declared on the contract"
required = false
style = "style.edge.data"
evidence = ["evidence.entity-contracts"]
limitations = []

[[edges]]
id = "edge.health-snapshot"
source = "actor.evidence-health"
target = "actor.market-state-entities"
category = "response"
contract = "contract.evidence-health-snapshot"
transport = "nautilus_signal"
correlation = "contract identity plus explicit source/instrument/request identity where verified"
sync_semantics = "verified_async"
delivery_claim = "delivery.signal-local"
enablement_condition = "metrics.entity_analysis.enabled"
authority_direction = "source publishes or commands; canonical ownership remains declared on the contract"
required = false
style = "style.edge.data"
evidence = ["evidence.entity-contracts"]
limitations = []

[[edges]]
id = "edge.entity-revision-semantic"
source = "actor.market-state-entities"
target = "future.semantic-events"
category = "publication"
contract = "contract.entity-revision"
transport = "nautilus_custom_data"
correlation = "contract identity plus explicit source/instrument/request identity where verified"
sync_semantics = "verified_async"
delivery_claim = "delivery.future-none"
enablement_condition = "future only"
authority_direction = "source publishes or commands; canonical ownership remains declared on the contract"
required = false
style = "style.edge.data"
evidence = ["evidence.roadmap"]
limitations = []

[[edges]]
id = "edge.semantic-opportunity"
source = "future.semantic-events"
target = "future.opportunities"
category = "publication"
contract = "contract.future-semantic-event"
transport = "not_applicable"
correlation = "contract identity plus explicit source/instrument/request identity where verified"
sync_semantics = "verified_async"
delivery_claim = "delivery.future-none"
enablement_condition = "future only"
authority_direction = "source publishes or commands; canonical ownership remains declared on the contract"
required = false
style = "style.edge.data"
evidence = ["evidence.roadmap"]
limitations = []

[[edges]]
id = "edge.opportunity-sir-loke"
source = "future.opportunities"
target = "future.sir-loke"
category = "projection"
contract = "contract.future-opportunity"
transport = "not_applicable"
correlation = "contract identity plus explicit source/instrument/request identity where verified"
sync_semantics = "verified_async"
delivery_claim = "delivery.future-none"
enablement_condition = "future only"
authority_direction = "source publishes or commands; canonical ownership remains declared on the contract"
required = false
style = "style.edge.projection"
evidence = ["evidence.roadmap"]
limitations = []

[[edges]]
id = "edge.evidence-sir-loke"
source = "actor.evidence-health"
target = "future.sir-loke"
category = "projection"
contract = "contract.evidence-health"
transport = "not_applicable"
correlation = "contract identity plus explicit source/instrument/request identity where verified"
sync_semantics = "verified_async"
delivery_claim = "delivery.future-none"
enablement_condition = "future only"
authority_direction = "source publishes or commands; canonical ownership remains declared on the contract"
required = false
style = "style.edge.projection"
evidence = ["evidence.roadmap"]
limitations = []

[[edges]]
id = "edge.sir-loke-operator"
source = "future.sir-loke"
target = "operator.markeitect"
category = "projection"
contract = "contract.future-advisory-intent"
transport = "not_applicable"
correlation = "contract identity plus explicit source/instrument/request identity where verified"
sync_semantics = "verified_async"
delivery_claim = "delivery.future-none"
enablement_condition = "future only"
authority_direction = "source publishes or commands; canonical ownership remains declared on the contract"
required = false
style = "style.edge.projection"
evidence = ["evidence.roadmap"]
limitations = []

[[edges]]
id = "edge.sir-loke-execution-intent"
source = "future.sir-loke"
target = "future.controlled-execution"
category = "control"
contract = "contract.future-advisory-intent"
transport = "not_applicable"
correlation = "contract identity plus explicit source/instrument/request identity where verified"
sync_semantics = "verified_async"
delivery_claim = "delivery.future-none"
enablement_condition = "future only"
authority_direction = "source publishes or commands; canonical ownership remains declared on the contract"
required = false
style = "style.edge.data"
evidence = ["evidence.roadmap"]
limitations = ["Advisory intent alone cannot authorize execution."]

[[edges]]
id = "edge.operator-execution-approval"
source = "operator.markeitect"
target = "future.controlled-execution"
category = "control"
contract = "contract.future-approval"
transport = "not_applicable"
correlation = "contract identity plus explicit source/instrument/request identity where verified"
sync_semantics = "verified_async"
delivery_claim = "delivery.future-none"
enablement_condition = "future only"
authority_direction = "source publishes or commands; canonical ownership remains declared on the contract"
required = false
style = "style.edge.data"
evidence = ["evidence.roadmap"]
limitations = ["Future separate approval boundary; no current execution."]

[[edges]]
id = "edge.persistence-ready-request"
source = "actor.system-control"
target = "actor.operational-persistence"
category = "query"
contract = "contract.persistence-ready-request"
transport = "nautilus_signal"
correlation = "contract identity plus explicit source/instrument/request identity where verified"
sync_semantics = "verified_async"
delivery_claim = "delivery.signal-local"
authority_direction = "source publishes or commands; canonical ownership remains declared on the contract"
required = true
style = "style.edge.data"
evidence = ["evidence.persistence"]
limitations = []

[[edges]]
id = "edge.persistence-ready-response"
source = "actor.operational-persistence"
target = "actor.system-control"
category = "readiness"
contract = "contract.persistence-ready"
transport = "nautilus_signal"
correlation = "contract identity plus explicit source/instrument/request identity where verified"
sync_semantics = "verified_async"
delivery_claim = "delivery.signal-local"
authority_direction = "source publishes or commands; canonical ownership remains declared on the contract"
required = true
style = "style.edge.data"
evidence = ["evidence.persistence"]
limitations = ["Implemented application-level handshake; documentation conflict remains recorded."]

[[edges]]
id = "edge.system-health-persistence"
source = "actor.system-control"
target = "actor.operational-persistence"
category = "publication"
contract = "contract.system-health"
transport = "nautilus_signal"
correlation = "contract identity plus explicit source/instrument/request identity where verified"
sync_semantics = "verified_async"
delivery_claim = "delivery.signal-local"
authority_direction = "source publishes or commands; canonical ownership remains declared on the contract"
required = true
style = "style.edge.data"
evidence = ["evidence.persistence"]
limitations = []

[[edges]]
id = "edge.persistence-queue"
source = "actor.operational-persistence"
target = "queue.persistence"
category = "queue_admission"
contract = "contract.operational-event"
transport = "thread_queue"
correlation = "contract identity plus explicit source/instrument/request identity where verified"
sync_semantics = "verified_async"
delivery_claim = "delivery.queue-bounded"
authority_direction = "source publishes or commands; canonical ownership remains declared on the contract"
required = true
style = "style.edge.persistence"
evidence = ["evidence.persistence"]
limitations = []

[[edges]]
id = "edge.queue-worker"
source = "queue.persistence"
target = "worker.persistence"
category = "queue_admission"
contract = "contract.operational-event"
transport = "thread_queue"
correlation = "contract identity plus explicit source/instrument/request identity where verified"
sync_semantics = "verified_async"
delivery_claim = "delivery.queue-bounded"
authority_direction = "source publishes or commands; canonical ownership remains declared on the contract"
required = true
style = "style.edge.persistence"
evidence = ["evidence.persistence"]
limitations = []

[[edges]]
id = "edge.worker-postgres"
source = "worker.persistence"
target = "store.postgres"
category = "persistence"
contract = "contract.operational-event"
transport = "postgres_write"
correlation = "contract identity plus explicit source/instrument/request identity where verified"
sync_semantics = "verified_async"
delivery_claim = "delivery.queue-bounded"
authority_direction = "source publishes or commands; canonical ownership remains declared on the contract"
required = true
style = "style.edge.persistence"
evidence = ["evidence.persistence"]
limitations = ["Stops at the application write boundary; database internals are unknown."]

[[edges]]
id = "edge.worker-result"
source = "worker.persistence"
target = "actor.operational-persistence"
category = "worker_result"
contract = "contract.persistence-result"
transport = "thread_queue"
correlation = "contract identity plus explicit source/instrument/request identity where verified"
sync_semantics = "verified_async"
delivery_claim = "delivery.queue-bounded"
authority_direction = "source publishes or commands; canonical ownership remains declared on the contract"
required = true
style = "style.edge.persistence"
evidence = ["evidence.persistence"]
limitations = []

[[edges]]
id = "edge.persistence-failure"
source = "actor.operational-persistence"
target = "actor.system-control"
category = "failure"
contract = "contract.component-failure"
transport = "nautilus_signal"
correlation = "contract identity plus explicit source/instrument/request identity where verified"
sync_semantics = "verified_async"
delivery_claim = "delivery.signal-local"
authority_direction = "source publishes or commands; canonical ownership remains declared on the contract"
required = true
style = "style.edge.failure"
evidence = ["evidence.persistence"]
limitations = []

[[edges]]
id = "edge.system-health-discord"
source = "actor.system-control"
target = "actor.discord-health"
category = "notification"
contract = "contract.system-health"
transport = "nautilus_signal"
correlation = "contract identity plus explicit source/instrument/request identity where verified"
sync_semantics = "verified_async"
delivery_claim = "delivery.signal-local"
enablement_condition = "discord.enabled"
authority_direction = "source publishes or commands; canonical ownership remains declared on the contract"
required = false
style = "style.edge.projection"
evidence = ["evidence.discord"]
limitations = []

[[edges]]
id = "edge.discord-queue"
source = "actor.discord-health"
target = "queue.discord"
category = "queue_admission"
contract = "contract.discord-notification"
transport = "thread_queue"
correlation = "contract identity plus explicit source/instrument/request identity where verified"
sync_semantics = "verified_async"
delivery_claim = "delivery.queue-bounded"
enablement_condition = "discord.enabled"
authority_direction = "source publishes or commands; canonical ownership remains declared on the contract"
required = false
style = "style.edge.persistence"
evidence = ["evidence.discord"]
limitations = []

[[edges]]
id = "edge.discord-worker"
source = "queue.discord"
target = "worker.discord"
category = "queue_admission"
contract = "contract.discord-notification"
transport = "thread_queue"
correlation = "contract identity plus explicit source/instrument/request identity where verified"
sync_semantics = "verified_async"
delivery_claim = "delivery.queue-bounded"
enablement_condition = "discord.enabled"
authority_direction = "source publishes or commands; canonical ownership remains declared on the contract"
required = false
style = "style.edge.persistence"
evidence = ["evidence.discord"]
limitations = []

[[edges]]
id = "edge.discord-external"
source = "worker.discord"
target = "projection.discord"
category = "notification"
contract = "contract.discord-notification"
transport = "external_http"
correlation = "contract identity plus explicit source/instrument/request identity where verified"
sync_semantics = "verified_async"
delivery_claim = "delivery.queue-bounded"
enablement_condition = "discord.enabled"
authority_direction = "source publishes or commands; canonical ownership remains declared on the contract"
required = false
style = "style.edge.projection"
evidence = ["evidence.discord"]
limitations = []

[[edges]]
id = "edge.resource-sample"
source = "actor.runtime-resources"
target = "actor.runtime-resource-health"
category = "publication"
contract = "contract.runtime-resource"
transport = "nautilus_signal"
correlation = "contract identity plus explicit source/instrument/request identity where verified"
sync_semantics = "verified_async"
delivery_claim = "delivery.signal-local"
enablement_condition = "runtime_resources.enabled"
authority_direction = "source publishes or commands; canonical ownership remains declared on the contract"
required = false
style = "style.edge.data"
evidence = ["evidence.resource-contracts"]
limitations = []

[[edges]]
id = "edge.resource-health-system"
source = "actor.runtime-resource-health"
target = "actor.system-control"
category = "publication"
contract = "contract.runtime-health"
transport = "nautilus_signal"
correlation = "contract identity plus explicit source/instrument/request identity where verified"
sync_semantics = "verified_async"
delivery_claim = "delivery.signal-local"
enablement_condition = "runtime_resources.health.enabled"
authority_direction = "source publishes or commands; canonical ownership remains declared on the contract"
required = false
style = "style.edge.data"
evidence = ["evidence.resource-contracts"]
limitations = []

[[styles]]
id = "style.theme"
semantic_role = "theme"
label = "Markeitech polished dark theme v2"

[[styles]]
id = "style.component.current"
semantic_role = "component_current"
label = "Implemented/current"

[[styles]]
id = "style.component.disabled"
semantic_role = "component_disabled"
label = "Implemented/conditional/disabled in named profile"

[[styles]]
id = "style.component.external"
semantic_role = "component_external"
label = "External boundary"

[[styles]]
id = "style.component.future"
semantic_role = "component_future"
label = "Future/proposed; not current"

[[styles]]
id = "style.edge.data"
semantic_role = "edge_data"
label = "Directed data, command, request, response, or control flow"

[[styles]]
id = "style.edge.persistence"
semantic_role = "edge_persistence"
label = "Queue, worker, or persistence flow"

[[styles]]
id = "style.edge.projection"
semantic_role = "edge_projection"
label = "External or diagnostic projection"

[[styles]]
id = "style.edge.failure"
semantic_role = "edge_failure"
label = "Failure or release path"

[[views]]
id = "view.current-runtime"
label = "Current V3 ES Runtime Topology"
kind = "current_runtime"
purpose = "Profile-specific implemented and enabled topology for the tracked V3 ES review profile."
profile = "profile.v3-es-minimal"
explicit_tombstones = []
include_temporal_status = ["current"]
include_implementation_state = ["implemented", "external"]
include_profile_enablement = ["enabled", "not_applicable"]
explicit_components = ["component.live-node", "component.data-engine", "component.cache", "component.canonical-calendar", "provider.interactive-brokers", "actor.system-control", "actor.session-state", "actor.evidence-health", "actor.historical-planner", "actor.watchlist", "actor.data-acquisition", "actor.current-state-historical-probe", "actor.operational-persistence", "queue.persistence", "worker.persistence", "store.postgres", "operator.markeitect"]
explicit_edges = ["edge.ib-native-bars", "edge.acquisition-subscribe", "edge.data-engine-live-callback", "edge.watchlist-demand", "edge.watchlist-status-request", "edge.acquisition-status-watchlist", "edge.acquisition-status-system", "edge.acquisition-stream-health", "edge.calendar-request-health", "edge.calendar-response-health", "edge.calendar-request-planner", "edge.calendar-response-planner", "edge.calendar-state-request-planner", "edge.calendar-state-response-planner", "edge.calendar-transition-health", "edge.calendar-transition-planner", "edge.calendar-state-request-probe", "edge.calendar-state-response-probe", "edge.calendar-transition-probe", "edge.current-state-probe-historical-demand", "edge.historical-plan-current-state-probe", "edge.calendar-transition-persistence", "edge.historical-plan-acquisition", "edge.native-historical-request", "edge.historical-provider-response", "edge.historical-batch-current-state-probe", "edge.historical-readiness-current-state-probe", "edge.persistence-ready-request", "edge.persistence-ready-response", "edge.system-health-persistence", "edge.persistence-queue", "edge.queue-worker", "edge.worker-postgres", "edge.worker-result", "edge.persistence-failure"]
direction = "left_to_right"
layout_engine = "dot"
routing = "spline"
pack_mode = "graph"
formats = ["svg", "png", "dot", "md"]
theme = "style.theme"
accessibility_companion_required = true
no_execution_banner = true
max_nodes = 20
max_edges = 44
max_cluster_depth = 2
target_width_px = 4600
target_height_px = 3200
minimum_text_size_pt = 10
evidence = ["evidence.current-status", "evidence.composition"]
limitations = ["Excludes profile-disabled actors and future/removed behavior.", "A visually connected provider boundary does not prove a current live connection or entitlement."]

[[views]]
id = "view.complete-inventory"
label = "Complete Component and Capability Inventory"
kind = "complete_inventory"
purpose = "Show implemented, conditional, disabled, external, and future components without implying runtime flow."
profile = "profile.v3-es-minimal"
explicit_tombstones = ["tombstone.visual-acceptance-actor", "tombstone.live-evidence-review-actor", "tombstone.legacy-session-state-contract", "tombstone.direct-historical-demand-edge", "tombstone.historical-release-edge"]
include_temporal_status = ["current", "historical", "future"]
include_implementation_state = ["implemented", "external", "removed", "rejected", "future"]
include_profile_enablement = ["enabled", "disabled", "not_applicable", "unknown"]
explicit_components = ["actor.system-control", "actor.session-state", "actor.evidence-health", "actor.discord-health", "actor.visual-debug", "actor.quote-quality", "actor.session-metrics", "actor.session-reference-entities", "actor.market-state-entities", "actor.market-structure-entities", "actor.historical-planner", "actor.watchlist", "actor.data-acquisition", "actor.historical-probe", "actor.current-state-historical-probe", "actor.native-consumer-probe", "actor.runtime-resources", "actor.runtime-resource-health", "actor.operational-persistence", "component.live-node", "component.data-engine", "component.cache", "component.canonical-calendar", "provider.interactive-brokers", "queue.persistence", "worker.persistence", "store.postgres", "queue.discord", "worker.discord", "projection.discord", "projection.visual-files", "operator.markeitect", "future.semantic-events", "future.opportunities", "future.sir-loke", "future.controlled-execution"]
explicit_edges = []
direction = "top_to_bottom"
layout_engine = "dot"
routing = "spline"
pack_mode = "array"
grid_columns = 4
rank_separation = 0.50
formats = ["svg", "png", "dot", "md"]
theme = "style.theme"
accessibility_companion_required = true
no_execution_banner = true
max_nodes = 41
max_edges = 1
max_cluster_depth = 2
target_width_px = 4300
target_height_px = 2650
minimum_text_size_pt = 10
evidence = ["evidence.current-status", "evidence.composition"]
limitations = ["Removed and rejected implementation identities are represented as tombstones in the manifest and companion record, not as active nodes.", "Configuration-gated subcapabilities are listed in the accessible Markdown companion under their owning components rather than duplicated as visual nodes.", "Inventory adjacency and clustering do not imply data flow."]

[[views]]
id = "view.provider-canonical-data"
label = "Provider to Canonical Data Flow"
kind = "provider_to_canonical_data"
purpose = "Show provider capability, native Nautilus boundary, acquisition ownership, historical/live convergence, metrics input, and evidence health."
profile = "profile.v3-es-minimal"
explicit_tombstones = []
include_temporal_status = ["current"]
include_implementation_state = ["implemented", "external"]
include_profile_enablement = ["enabled", "not_applicable"]
explicit_components = ["provider.interactive-brokers", "component.data-engine", "component.cache", "component.canonical-calendar", "actor.watchlist", "actor.data-acquisition", "actor.historical-planner", "actor.session-metrics", "actor.session-state", "actor.evidence-health"]
explicit_edges = ["edge.ib-native-bars", "edge.acquisition-subscribe", "edge.data-engine-live-callback", "edge.data-engine-metric-bars", "edge.watchlist-demand", "edge.watchlist-status-request", "edge.acquisition-status-watchlist", "edge.acquisition-stream-health", "edge.calendar-request-health", "edge.calendar-response-health", "edge.calendar-request-metrics", "edge.calendar-response-metrics", "edge.calendar-request-planner", "edge.calendar-response-planner", "edge.calendar-state-request-planner", "edge.calendar-state-response-planner", "edge.calendar-transition-health", "edge.calendar-transition-planner", "edge.historical-demand-planner", "edge.historical-plan-acquisition", "edge.native-historical-request", "edge.historical-provider-response", "edge.historical-batch", "edge.historical-readiness"]
direction = "left_to_right"
layout_engine = "dot"
routing = "spline"
pack_mode = "graph"
rank_separation = 0.35
formats = ["svg", "png", "dot", "md"]
theme = "style.theme"
accessibility_companion_required = true
no_execution_banner = true
max_nodes = 12
max_edges = 28
max_cluster_depth = 2
target_width_px = 2400
target_height_px = 2100
minimum_text_size_pt = 10
evidence = ["evidence.current-status", "evidence.composition"]
limitations = ["Exact IB request methods, callbacks, qualification, entitlement, delivery mode, request IDs, and cancellation are unknown.", "Native subscription command issuance is not provider acknowledgement.", "Historical and live observations are distinct lineage sources; general equality and gap-free delivery are not claimed."]

[[views]]
id = "view.metrics-entities-intelligence"
label = "Metrics, Entities, Intelligence, and State"
kind = "metrics_entities_intelligence"
purpose = "Show implemented metrics/state and profile-disabled entity capabilities with their canonical contracts."
profile = "profile.v3-es-minimal"
explicit_tombstones = []
include_temporal_status = ["current"]
include_implementation_state = ["implemented"]
include_profile_enablement = ["enabled", "disabled"]
explicit_components = ["component.data-engine", "component.canonical-calendar", "actor.watchlist", "actor.session-state", "actor.evidence-health", "actor.quote-quality", "actor.session-metrics", "actor.historical-planner", "actor.session-reference-entities", "actor.market-state-entities", "actor.market-structure-entities", "actor.visual-debug"]
explicit_edges = ["edge.data-engine-metric-bars", "edge.calendar-request-health", "edge.calendar-response-health", "edge.calendar-request-metrics", "edge.calendar-response-metrics", "edge.calendar-request-planner", "edge.calendar-response-planner", "edge.calendar-transition-health", "edge.calendar-transition-planner", "edge.completed-bars-visual", "edge.metrics-visual", "edge.completed-bars-session-entities", "edge.metrics-market-state", "edge.metrics-market-structure", "edge.recency-profile", "edge.health-snapshot-request", "edge.health-snapshot"]
direction = "left_to_right"
layout_engine = "dot"
routing = "spline"
pack_mode = "graph"
node_separation = 0.30
formats = ["svg", "png", "dot", "md"]
theme = "style.theme"
accessibility_companion_required = true
no_execution_banner = true
max_nodes = 14
max_edges = 24
max_cluster_depth = 2
target_width_px = 3350
target_height_px = 1500
minimum_text_size_pt = 10
evidence = ["evidence.current-status", "evidence.composition"]
limitations = ["Entity actors and quote-quality metrics are implemented but disabled in the named profile.", "Dependencies do not imply causality, confidence, ranking, advice, or trading intent."]

[[views]]
id = "view.persistence-audit-projections"
label = "Persistence, Operational Audit, and External Projections"
kind = "persistence_audit_projections"
purpose = "Separate bounded operational persistence from optional notifications and diagnostic projections."
profile = "profile.v3-es-minimal"
explicit_tombstones = []
include_temporal_status = ["current"]
include_implementation_state = ["implemented", "external"]
include_profile_enablement = ["enabled", "disabled", "not_applicable"]
explicit_components = ["actor.system-control", "actor.session-state", "actor.operational-persistence", "queue.persistence", "worker.persistence", "store.postgres", "actor.discord-health", "queue.discord", "worker.discord", "projection.discord", "actor.visual-debug", "projection.visual-files", "operator.markeitect"]
explicit_edges = ["edge.calendar-transition-persistence", "edge.persistence-ready-request", "edge.persistence-ready-response", "edge.system-health-persistence", "edge.persistence-queue", "edge.queue-worker", "edge.worker-postgres", "edge.worker-result", "edge.persistence-failure", "edge.system-health-discord", "edge.discord-queue", "edge.discord-worker", "edge.discord-external", "edge.visual-files", "edge.visual-review"]
direction = "left_to_right"
layout_engine = "dot"
routing = "spline"
pack_mode = "graph"
formats = ["svg", "png", "dot", "md"]
theme = "style.theme"
accessibility_companion_required = true
no_execution_banner = true
max_nodes = 16
max_edges = 18
max_cluster_depth = 2
target_width_px = 4000
target_height_px = 1250
minimum_text_size_pt = 10
evidence = ["evidence.current-status", "evidence.composition"]
limitations = ["PostgreSQL stores approved operational facts, not raw provider observations by default.", "Queue admission is not storage and storage does not acknowledge the original event producer.", "Discord and visual files are projections and never create canonical truth.", "Database-internal details are intentionally unknown."]

[[views]]
id = "view.future-governed-boundaries"
label = "Future Governed Advisory and Controlled Boundaries"
kind = "future_governed_boundaries"
purpose = "Isolate future semantic events, plural opportunities, Sir Loke advisory intent, Markeitect approval, and a separately governed execution boundary."
profile = "profile.v3-es-minimal"
explicit_tombstones = []
include_temporal_status = ["current", "future"]
include_implementation_state = ["implemented", "external", "future"]
include_profile_enablement = ["not_applicable", "enabled"]
explicit_components = ["actor.evidence-health", "actor.market-state-entities", "future.semantic-events", "future.opportunities", "future.sir-loke", "operator.markeitect", "future.controlled-execution"]
explicit_edges = ["edge.entity-revision-semantic", "edge.semantic-opportunity", "edge.opportunity-sir-loke", "edge.evidence-sir-loke", "edge.sir-loke-operator", "edge.sir-loke-execution-intent", "edge.operator-execution-approval"]
direction = "top_to_bottom"
layout_engine = "dot"
routing = "spline"
pack_mode = "graph"
formats = ["svg", "png", "dot", "md"]
theme = "style.theme"
accessibility_companion_required = true
no_execution_banner = true
max_nodes = 10
max_edges = 10
max_cluster_depth = 2
target_width_px = 1500
target_height_px = 1950
minimum_text_size_pt = 10
evidence = ["evidence.current-status", "evidence.composition"]
limitations = ["Every purple/dashed boundary is proposed or future and not current runtime behavior.", "Sir Loke is unrelated to the development-time Kite advisor council.", "No current order submission, execution authority, or execution implementation exists."]

[[tombstones]]
id = "tombstone.visual-acceptance-actor"
label = "Visual Acceptance Actor"
former_kind = "markeitech_actor"
former_boundary = "boundary.historical"
disposition = "rejected"
removed_at_commit = "c6fe2ad89ae2da077d08c55998cc9ff639c5f0ce"
evidence = ["evidence.current-status"]
limitations = ["Historical only; never current runtime behavior."]

[[tombstones]]
id = "tombstone.live-evidence-review-actor"
label = "Live Evidence Review Actor"
former_kind = "markeitech_actor"
former_boundary = "boundary.historical"
disposition = "rejected"
removed_at_commit = "c6fe2ad89ae2da077d08c55998cc9ff639c5f0ce"
evidence = ["evidence.current-status"]
limitations = ["Historical only; never current runtime behavior."]

[[tombstones]]
id = "tombstone.legacy-session-state-contract"
label = "Legacy SessionStateEvent signal"
former_kind = "contract"
former_boundary = "boundary.intelligence"
disposition = "removed"
removed_at_commit = "b09e7ddf75b730f23aefa4c263d64b5dc3961979"
replacement = "contract.calendar-transition"
evidence = ["evidence.current-status", "evidence.calendar-contracts"]
limitations = ["Historical identity only; current consumers use definition-identified calendar custom data."]

[[tombstones]]
id = "tombstone.direct-historical-demand-edge"
label = "Direct Session Metrics to Data Acquisition historical demand"
former_kind = "edge"
former_boundary = "boundary.acquisition"
disposition = "removed"
removed_at_commit = "b09e7ddf75b730f23aefa4c263d64b5dc3961979"
replacement = "edge.historical-demand-planner"
evidence = ["evidence.current-status", "evidence.historical-planner"]
limitations = ["Historical topology only; Data Acquisition now receives exact plans rather than semantic calendar-relative demand."]

[[tombstones]]
id = "tombstone.historical-release-edge"
label = "Session Metrics historical execution release"
former_kind = "edge"
former_boundary = "boundary.acquisition"
disposition = "removed"
removed_at_commit = "b09e7ddf75b730f23aefa4c263d64b5dc3961979"
evidence = ["evidence.historical-contracts", "evidence.historical-planner"]
limitations = ["No current SessionMetricsActor publication implements this former represented edge."]

[[authority_conflicts]]
id = "conflict.persistence-readiness-docs"
label = "Persistence-readiness implementation and documentation mismatch"
affected_ids = ["actor.system-control", "actor.operational-persistence", "contract.persistence-ready-request", "contract.persistence-ready"]
evidence = ["evidence.current-status", "evidence.persistence"]
required_decision = "Represent the implemented application-level request/response handshake and retain this visible limitation until current-status wording is reconciled."
status = "accepted_diagram_treatment_pending_document_reconciliation"
limitations = ["Does not establish PostgreSQL internal readiness, durability, restore acceptance, or connected-run success."]
