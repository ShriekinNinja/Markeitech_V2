policy:
  allow_implicit_invocation: false

interface:
  display_name: "Python Runtime"
  short_description: "Focused python runtime guidance"
  default_prompt: "Use $kite:markeitech-python-runtime-expert to answer this scoped question using the relevant evidence."
