policy:
  allow_implicit_invocation: false

interface:
  display_name: "Kite Focused Skills"
  short_description: "Use focused skills for the task at hand"
  default_prompt: "Use $kite:markeitech-advisor-router for this task. Apply only the relevant skills and keep the work scoped to the requested outcome."
