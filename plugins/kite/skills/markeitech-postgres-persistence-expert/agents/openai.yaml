policy:
  allow_implicit_invocation: false

interface:
  display_name: "Postgres Persistence"
  short_description: "Focused postgres persistence guidance"
  default_prompt: "Use $kite:markeitech-postgres-persistence-expert to answer this scoped question using the relevant evidence."
